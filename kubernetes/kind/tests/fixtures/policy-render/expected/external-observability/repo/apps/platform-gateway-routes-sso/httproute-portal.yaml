apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: portal
spec:
  hostnames:
    - portal.obs.example.test
  rules:
    - backendRefs:
        - name: oauth2-proxy-backstage
          namespace: sso
