apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # NGINX Gateway Fabric controller/runtime resources. CRDs are installed by a
  # separate Argo app so clean-cluster bootstrap does not rely on mid-sync API
  # discovery refreshing.
  - deploy.yaml
