[project]
name = "platform-idp-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
exclude-newer = "7 days"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[project.scripts]
idp-mcp = "idp_mcp.server:main"
