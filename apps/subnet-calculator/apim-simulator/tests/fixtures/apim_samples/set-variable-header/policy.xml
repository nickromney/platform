<policies>
  <inbound>
    <set-variable name="mode" value="{query:mode}" />
    <set-header name="x-mode" exists-action="override">
      <value>{var:mode}</value>
    </set-header>
  </inbound>
  <backend />
  <outbound />
  <on-error />
</policies>
