apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-mcp
spec:
  template:
    spec:
      containers:
        - name: platform-mcp
          image: localhost:30090/platform/platform-mcp:latest
