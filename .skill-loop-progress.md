# Skill Loop Progress

## Skill: improve-codebase-architecture using tdd

## Target: /Users/nickromney/Developer/personal/platform

## Started: 2026-06-11T14:28:57Z

## Status: ACTIVE

## Stop Conditions

- Continue while a scoped architecture deepening has a public behavior contract to improve.
- Stop only after a requirement-by-requirement audit finds no further meaningful improvements remain.
- Keep each pass behavior-preserving, test-led, and focused on a named module/interface boundary.

## Completed Passes

### Pass 57 - Shared post-apply verification runner adapter - 2026-06-11T20:31:33Z

- Files changed: tests/kubernetes-post-apply-verification-runner.bats, tests/kubernetes-post-apply-verification-plan.bats, kubernetes/scripts/run-post-apply-verification.sh, kubernetes/kind/Makefile, kubernetes/lima/Makefile, kubernetes/slicer/Makefile
- Changes: Added a shared post-apply verification runner module that reads planned step names from stdin and maps them to the correct variant Make adapter invocations, preserving Lima's unstaged `configure-k3s-apiserver-oidc` call and Slicer's staged configure call. The three variant `apply` recipes now pipe planner output to one runner interface instead of owning duplicate `case` blocks.
- Red test: `bats tests/kubernetes-post-apply-verification-runner.bats` failed before implementation with command-not-found because `kubernetes/scripts/run-post-apply-verification.sh` did not exist.
- Verification: `bats tests/kubernetes-post-apply-verification-runner.bats tests/kubernetes-post-apply-verification-plan.bats`; `bash -n kubernetes/scripts/plan-post-apply-verification.sh kubernetes/scripts/run-post-apply-verification.sh`; `shellcheck kubernetes/scripts/plan-post-apply-verification.sh kubernetes/scripts/run-post-apply-verification.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/plan-post-apply-verification.sh --path kubernetes/scripts/run-post-apply-verification.sh`; `scripts/check-make-target-surfaces.sh --execute`; `make -C kubernetes/kind help`; `make -C kubernetes/lima help`; `make -C kubernetes/slicer help`; `rg -n "RUN_POST_APPLY_VERIFICATION|Unknown post-apply verification step|post_apply_plan|while IFS= read -r post_apply_step" kubernetes/kind/Makefile kubernetes/lima/Makefile kubernetes/slicer/Makefile tests/kubernetes-post-apply-verification-plan.bats`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 56 - Shared post-apply verification plan - 2026-06-11T20:26:25Z

- Files changed: tests/kubernetes-post-apply-verification-plan.bats, kubernetes/scripts/plan-post-apply-verification.sh, kubernetes/kind/Makefile, kubernetes/lima/Makefile, kubernetes/slicer/Makefile
- Changes: Added a shared post-apply verification planner module whose interface is `--variant-json`, `--stage`, and ordered `--var-file` inputs, then routed Kind, Lima, and Slicer `apply` recipes through the planner instead of duplicating gateway/headlamp/SSO post-apply policy in each runtime adapter.
- Red test: `bats tests/kubernetes-post-apply-verification-plan.bats --filter 'post-apply verification plan is sourced from variant contract and tfvars'` failed before implementation because `kubernetes/scripts/plan-post-apply-verification.sh` did not exist.
- Verification: `bats tests/kubernetes-post-apply-verification-plan.bats --filter 'post-apply verification plan is sourced from variant contract and tfvars'`; `bats tests/kubernetes-post-apply-verification-plan.bats`; `bash -n kubernetes/scripts/plan-post-apply-verification.sh`; `shellcheck kubernetes/scripts/plan-post-apply-verification.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/plan-post-apply-verification.sh`; `scripts/check-make-target-surfaces.sh --execute`; `rg -n "enable_gateway_tls|enable_headlamp|PLAN_POST_APPLY_VERIFICATION|post_apply_plan" kubernetes/kind/Makefile kubernetes/lima/Makefile kubernetes/slicer/Makefile`; `git diff --check`. Attempted `make -n -C kubernetes/{kind,lima,slicer} apply STAGE=900 AUTO_APPROVE=1` was stopped because recursive Make recipes crossed into live `terragrunt apply`; no apply process remained afterward, and ignored runtime state under `terraform/.run` was touched.
- Verdict: PRODUCTIVE

### Pass 55 - Shared diagnostic dispatch for show-urls - 2026-06-11T20:16:59Z

- Files changed: tests/kubernetes-diagnostic-dispatch.bats, kubernetes/scripts/run-diagnostic-check.sh, kubernetes/kind/Makefile, kubernetes/lima/Makefile, kubernetes/slicer/Makefile
- Changes: Extended the shared diagnostic dispatch module from `check-health` to `show-urls`, preserving each variant's local preflight gates while moving the shared `check-cluster-health.sh --show-urls` invocation behind `run-diagnostic-check.sh`.
- Red test: `bats tests/kubernetes-diagnostic-dispatch.bats --filter 'show-urls'` failed before implementation because the three variant `show-urls` targets called `check-cluster-health.sh --show-urls` directly and the dispatcher rejected `--action show-urls`.
- Verification: `bats tests/kubernetes-diagnostic-dispatch.bats --filter 'show-urls'`; `bats tests/kubernetes-diagnostic-dispatch.bats`; `make -n -C kubernetes/kind show-urls STAGE=900`; `make -n -C kubernetes/lima show-urls STAGE=900`; `make -n -C kubernetes/slicer show-urls STAGE=900`; `bash -n kubernetes/scripts/run-diagnostic-check.sh`; `shellcheck kubernetes/scripts/run-diagnostic-check.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/run-diagnostic-check.sh`; `bats kubernetes/kind/tests/makefile.bats --filter 'show-urls|check-health'`; `bats kubernetes/lima/tests/makefile.bats --filter 'show-urls|check-health'`; `bats kubernetes/slicer/tests/makefile.bats --filter 'show-urls|check-health'`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 54 - Shared IDP preview action catalog - 2026-06-11T20:12:56Z

- Files changed: kubernetes/scripts/idp-preview-action-catalog.sh, mk/k8s-idp-preview.mk, scripts/platform-status.sh, tests/local-idp-contracts.bats
- Changes: Added a shared IDP preview action catalog so the Make preview adapter and `platform-status` action projection consume the same target metadata, dry-run text, and run-command text instead of duplicating the preview target interface.
- Red test: `bats tests/local-idp-contracts.bats --filter 'IDP preview status actions and Make dry-runs share the preview catalog'` failed before implementation because `kubernetes/scripts/idp-preview-action-catalog.sh` did not exist.
- Verification: `bats tests/local-idp-contracts.bats --filter 'IDP preview status actions and Make dry-runs share the preview catalog'`; `bats tests/local-idp-contracts.bats --filter 'IDP preview'`; `bats tests/platform-status.bats --filter 'shared IDP preview actions'`; `make -C kubernetes/kind idp-api DRY_RUN=1`; `make -C kubernetes/lima backstage DRY_RUN=1`; `make -C kubernetes/slicer idp-mcp DRY_RUN=1`; `bats tests/local-idp-contracts.bats`; `bats tests/platform-status.bats`; `bash -n kubernetes/scripts/idp-preview-action-catalog.sh scripts/platform-status.sh`; `shellcheck kubernetes/scripts/idp-preview-action-catalog.sh scripts/platform-status.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/idp-preview-action-catalog.sh --path scripts/platform-status.sh`; `scripts/check-make-target-surfaces.sh --execute`; `kubernetes/scripts/idp-preview-action-catalog.sh --records`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 53 - Shared IDP preview actions in platform status - 2026-06-11T20:05:58Z

- Files changed: scripts/platform-status.sh, tests/platform-status.bats
- Changes: Added a behavior contract proving `platform-status` projects the shared local IDP preview targets for Kind, Lima, and Slicer, then introduced a shared action-projection helper so the status read model gets those actions from one interface instead of per-variant copies.
- Red test: `bats tests/platform-status.bats --filter 'shared IDP preview actions'` failed before implementation because Lima and Slicer did not expose the shared IDP preview actions in `.actions`.
- Verification: `bats tests/platform-status.bats --filter 'shared IDP preview actions'`; `scripts/platform-status.sh --execute --output json | jq -e '.actions | map(.id) | index("lima-idp-api") and index("slicer-idp-mcp")'`; `bash -n scripts/platform-status.sh`; `bats tests/platform-status.bats`; `bats tests/local-idp-contracts.bats`; `make -C kubernetes/kind idp-api DRY_RUN=1`; `make -C kubernetes/lima idp-api DRY_RUN=1`; `make -C kubernetes/slicer idp-api DRY_RUN=1`; `shellcheck scripts/platform-status.sh`; `scripts/check-bash32-compat.sh --execute --path scripts/platform-status.sh`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 52 - Shared local IDP preview Make interface - 2026-06-11T20:00:09Z

- Files changed: tests/local-idp-contracts.bats, mk/k8s-idp-preview.mk, kubernetes/kind/Makefile, kubernetes/lima/Makefile, kubernetes/slicer/Makefile
- Changes: Added a behavior contract requiring Kind, Lima, and Slicer to expose the same local IDP preview targets, then moved the duplicated Kind/Lima preview recipes into a shared Kubernetes Make include and wired Slicer into the interface with its own runtime label.
- Red test: `bats tests/local-idp-contracts.bats --filter 'IDP Make targets'` failed before implementation because Slicer did not provide the IDP preview targets.
- Verification: `bats tests/local-idp-contracts.bats --filter 'IDP Make targets'`; `make -C kubernetes/slicer idp-api DRY_RUN=1`; `make -C kubernetes/slicer backstage DRY_RUN=1`; `bats tests/local-idp-contracts.bats`; `make -C kubernetes/slicer help | rg 'idp-api|backstage|idp-sdk|idp-mcp'`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 1 - App wrapper Makefile interface depth - 2026-06-11T14:28:57Z

- Files changed: apps/Makefile, tests/apps-makefile.bats, tests/app_contracts.py
- Changes: Added a behavior test proving the apps aggregator delegates to wrappers whose `MAKE_KNOWN_GOALS` interface is formatted across continued lines, then made runtime discovery and the shared Python contract parser continuation-aware.
- Red test: `bats --filter 'apps wrapper target discovery supports continued MAKE_KNOWN_GOALS declarations' tests/apps-makefile.bats` failed before implementation.
- Verification: `bats --filter 'apps wrapper target discovery supports continued MAKE_KNOWN_GOALS declarations' tests/apps-makefile.bats`; `bats tests/apps-makefile.bats tests/app-layout-consistency.bats tests/sentiment-makefile.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 2 - Declared app wrapper target surface - 2026-06-11T14:32:04Z

- Files changed: mk/app-common.mk, mk/app-common-update.mk, tests/app-layout-consistency.bats
- Changes: Added a behavior test proving a minimal `mk/app-common.mk` wrapper exposes only its declared non-compose goals, then moved the default `update` recipe into a conditional include so text-driven help and make execution do not leak undeclared targets.
- Red test: `bats --filter 'common app wrapper exposes only declared non-compose targets' tests/app-layout-consistency.bats` failed before implementation.
- Verification: `bats --filter 'common app wrapper exposes only declared non-compose targets' tests/app-layout-consistency.bats`; `bats tests/app-layout-consistency.bats tests/apps-makefile.bats tests/sentiment-makefile.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 3 - Declared compose lifecycle target surface - 2026-06-11T14:34:41Z

- Files changed: mk/app-common-compose.mk, mk/app-common-compose-prereqs.mk, mk/app-common-compose-smoke.mk, mk/app-common-compose-down.mk, mk/app-common-compose-logs.mk, mk/app-common-compose-ps.mk, tests/app-layout-consistency.bats
- Changes: Added a behavior test proving a compose-backed `mk/app-common.mk` wrapper exposes only declared compose lifecycle goals, then split compose lifecycle recipes into conditional per-target includes so help and execution stay aligned with `MAKE_KNOWN_GOALS`.
- Red test: `bats --filter 'common app wrapper exposes only declared compose lifecycle targets' tests/app-layout-consistency.bats` failed before implementation.
- Verification: `bats --filter 'common app wrapper exposes only declared compose lifecycle targets' tests/app-layout-consistency.bats`; `bats tests/app-layout-consistency.bats tests/apps-makefile.bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 4 - Private compose lifecycle prerequisites - 2026-06-11T14:38:33Z

- Files changed: mk/app-common-compose.mk, mk/app-common-compose-prereqs.mk, mk/app-common-compose-prereqs-private.mk, mk/app-common-compose-down.mk, mk/app-common-compose-logs.mk, mk/app-common-compose-ps.mk, tests/app-layout-consistency.bats
- Changes: Added a behavior test proving compose lifecycle commands can use the shared prerequisite check without declaring the public `prereqs` goal, then introduced a private `app-common-compose-prereqs` target for `down`, `logs`, and `ps` while keeping public `prereqs` as an optional declared alias.
- Red test: `bats --filter 'common app compose lifecycle targets use private prerequisites when prereqs is undeclared' tests/app-layout-consistency.bats` failed before implementation.
- Verification: `bats --filter 'common app compose lifecycle targets use private prerequisites when prereqs is undeclared' tests/app-layout-consistency.bats`; `bats tests/app-layout-consistency.bats tests/apps-makefile.bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 5 - Declared goals require real rules - 2026-06-11T14:40:49Z

- Files changed: mk/app-common.mk, mk/app-common-compose-prereqs.mk, mk/app-common-compose-smoke.mk, mk/app-common-compose-down.mk, mk/app-common-compose-logs.mk, mk/app-common-compose-ps.mk, mk/app-common-update.mk, tests/app-layout-consistency.bats
- Changes: Added a behavior test proving a declared but unimplemented wrapper goal fails through the unknown-goal path, then removed the broad `.PHONY: $(MAKE_KNOWN_GOALS)` mask and moved phony declarations onto concrete shared targets only.
- Red test: `bats --filter 'common app wrapper does not turn declared missing goals into empty successes' tests/app-layout-consistency.bats` failed before implementation.
- Verification: `bats --filter 'common app wrapper does not turn declared missing goals into empty successes' tests/app-layout-consistency.bats`; `bats tests/app-layout-consistency.bats tests/apps-makefile.bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats tests/make-target-surfaces.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 6 - Declared goals gate help and execution - 2026-06-11T14:45:15Z

- Files changed: mk/common.mk, tests/app-layout-consistency.bats
- Changes: Added a behavior test proving a documented wrapper-local public rule is hidden from help and rejected unless declared in `MAKE_KNOWN_GOALS`, then made common help filter against known goals and added a known-goal guard with internal helper allowances.
- Red test: `bats --filter 'common app wrapper rejects public rules that are not declared goals' tests/app-layout-consistency.bats` failed before implementation.
- Verification: `bats --filter 'common app wrapper rejects public rules that are not declared goals' tests/app-layout-consistency.bats`; `bats tests/app-layout-consistency.bats tests/apps-makefile.bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats tests/make-target-surfaces.bats tests/makefile.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 7 - Dynamic app aggregate delegation targets - 2026-06-11T14:48:58Z

- Files changed: apps/Makefile, tests/apps-makefile.bats, tests/app_contracts.py
- Changes: Added a behavior test proving a new wrapper that declares `compose-smoke` is immediately reachable through `make -C apps compose-smoke-<app>`, then replaced hardcoded per-app compose smoke and unit-test recipes with guarded pattern rules derived from wrapper declarations.
- Red test: `bats --filter 'apps dynamic compose-smoke app target delegates to matching wrapper' tests/apps-makefile.bats` failed before implementation.
- Verification: `bats tests/apps-makefile.bats`; `bats tests/app-layout-consistency.bats tests/apps-makefile.bats tests/make-target-surfaces.bats tests/makefile.bats`; `bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 8 - Evaluated wrapper capability discovery - 2026-06-11T14:52:01Z

- Files changed: apps/Makefile, tests/apps-makefile.bats, tests/app_contracts.py
- Changes: Added a behavior test proving aggregate delegation sees `MAKE_KNOWN_GOALS += update`, then replaced the apps aggregator's textual assignment parser with make database discovery so wrapper capabilities follow make's evaluated variable value.
- Red test: `bats --filter 'apps wrapper target discovery uses evaluated MAKE_KNOWN_GOALS additions' tests/apps-makefile.bats` failed before implementation.
- Verification: `bats tests/apps-makefile.bats`; `bats tests/app-layout-consistency.bats tests/apps-makefile.bats tests/make-target-surfaces.bats tests/makefile.bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 9 - Evaluated wrapper helper parity - 2026-06-11T14:54:13Z

- Files changed: tests/apps-makefile.bats, tests/app_contracts.py
- Changes: Added a behavior test proving the Python app wrapper helper sees `MAKE_KNOWN_GOALS += app-test`, then switched `app_wrapper_names_with_target` to query make's evaluated database with the same internal no-op target used by runtime aggregation.
- Red test: `bats --filter 'apps Makefile contract helpers use evaluated MAKE_KNOWN_GOALS additions' tests/apps-makefile.bats` failed before implementation.
- Verification: `bats tests/apps-makefile.bats`; `bats tests/app-layout-consistency.bats tests/apps-makefile.bats tests/make-target-surfaces.bats tests/makefile.bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 10 - Evaluated wrapper layout contract - 2026-06-11T14:56:18Z

- Files changed: tests/app-layout-consistency.bats, tests/app_contracts.py
- Changes: Updated the common-wrapper fixture to declare `app-prereqs` via `MAKE_KNOWN_GOALS += app-prereqs`, then switched `app_wrapper_contract_violations` to use evaluated make goals so layout contracts match runtime wrapper semantics.
- Red test: `bats --filter 'in-scope Go app wrappers share the common Makefile interface' tests/app-layout-consistency.bats` failed before implementation.
- Verification: `bats --filter 'in-scope Go app wrappers share the common Makefile interface' tests/app-layout-consistency.bats`; `bats tests/app-layout-consistency.bats tests/apps-makefile.bats tests/make-target-surfaces.bats tests/makefile.bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 11 - Pattern targets visible in shared help - 2026-06-11T15:00:14Z

- Files changed: mk/common.mk, tests/app_contracts.py
- Changes: Added an apps help contract for `compose-smoke-%` and `%-test`, then taught the shared help parser to recognize `%` in documented target names so generic app wrapper pattern rules are visible in `make -C apps help`.
- Red test: `bats --filter 'apps make help exposes the Trivy security workflow' tests/apps-makefile.bats` failed before implementation.
- Verification: `bats --filter 'apps make help exposes the Trivy security workflow' tests/apps-makefile.bats`; `make -C apps help`; `bats tests/apps-makefile.bats tests/makefile.bats tests/make-target-surfaces.bats`; `bats tests/app-layout-consistency.bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 12 - Evaluated make surface audit - 2026-06-11T15:03:13Z

- Files changed: scripts/check-make-target-surfaces.sh, tests/make-target-surfaces.bats
- Changes: Added a target-surface contract for `MAKE_KNOWN_GOALS += missing-real-target`, then changed the repo-wide audit to derive both phony targets and known goals from the loaded make database instead of only direct source-file assignments.
- Red test: `bats --filter 'target surface audit checks evaluated MAKE_KNOWN_GOALS additions' tests/make-target-surfaces.bats` failed before implementation.
- Verification: `bats --filter 'target surface audit checks evaluated MAKE_KNOWN_GOALS additions' tests/make-target-surfaces.bats`; `bash -n scripts/check-make-target-surfaces.sh`; `bats tests/make-target-surfaces.bats tests/apps-makefile.bats tests/app-layout-consistency.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 13 - Evaluated make goal adapter - 2026-06-11T15:09:52Z

- Files changed: scripts/make-known-goals.sh, apps/Makefile, scripts/check-make-target-surfaces.sh, tests/make-target-surfaces.bats, tests/app_contracts.py
- Changes: Added a repo-owned evaluated-goal adapter that hides noop Makefile creation, fixture env setup, make database loading, and `MAKE_KNOWN_GOALS` extraction, then routed the apps aggregator, Python contracts, and target-surface audit through it.
- Red test: `bats tests/make-target-surfaces.bats` failed before implementation on `make known goals helper reports evaluated MAKE_KNOWN_GOALS additions` because `scripts/make-known-goals.sh` did not exist.
- Verification: `bats tests/apps-makefile.bats tests/make-target-surfaces.bats`; `scripts/check-make-target-surfaces.sh --execute`; `bash -n scripts/make-known-goals.sh scripts/check-make-target-surfaces.sh`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`; `bats tests/app-layout-consistency.bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats tests/makefile.bats`; `make -C apps help`; `scripts/make-known-goals.sh --dir apps/subnetcalc --execute`.
- Verdict: PRODUCTIVE

### Pass 14 - Shared app-core delegation targets - 2026-06-11T15:17:25Z

- Files changed: mk/app-common.mk, mk/app-common-core.mk, tests/app-layout-consistency.bats, apps/apim-simulator/Makefile, apps/chatgpt-sim/Makefile, apps/idp-core/Makefile, apps/langfuse-demos/Makefile, apps/platform-mcp/Makefile, apps/sentiment/Makefile, apps/subnetcalc/Makefile
- Changes: Added `mk/app-common-core.mk` so declared `app-help`, `app-test`, `app-js-check`, and `app-build` targets delegate to the app core from one shared module, then removed the duplicated generic target bodies from wrappers while leaving app-specific run, build, test, URL, SSO smoke, and compatibility targets local.
- Red test: `bats --filter 'common app wrapper delegates declared app core targets' tests/app-layout-consistency.bats` failed before implementation because the shared wrapper did not expose `app-help`.
- Verification: `bats --filter 'common app wrapper delegates declared app core targets' tests/app-layout-consistency.bats`; `bats tests/app-layout-consistency.bats tests/apps-makefile.bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats tests/make-target-surfaces.bats`; `scripts/check-make-target-surfaces.sh --execute` after serial rerun; `git diff --check`; `python3 -B -m py_compile tests/app_contracts.py`; `bash -n scripts/make-known-goals.sh scripts/check-make-target-surfaces.sh`; `make -C apps help`.
- Verdict: PRODUCTIVE

### Pass 15 - Shared Go app-core Make module - 2026-06-11T15:30:18Z

- Files changed: mk/go-app-core.mk, apps/apim-simulator/app/Makefile, apps/chatgpt-sim/app/Makefile, apps/idp-core/app/Makefile, apps/langfuse-demos/app/Makefile, apps/platform-mcp/app/Makefile, apps/sentiment/app/Makefile, apps/subnetcalc/app/Makefile, tests/app_contracts.py, tests/validate-docker-optimization-contracts.bats
- Changes: Added `mk/go-app-core.mk` so Go app cores share `test`, `build`, `build-linux`, `clean`, optional `js-check`, and optional `static-dist` build policy, while each app core keeps its help, run, prereq, and update behavior local.
- Red test: `bats tests/validate-docker-optimization-contracts.bats --filter 'local Go app Makefiles share common Go app core module'` failed before implementation with missing shared include and duplicated shared recipe violations.
- Verification: `bats tests/validate-docker-optimization-contracts.bats --filter 'local Go app Makefiles share common Go app core module|local Go app Makefiles create their binary output directory|local Go app Makefiles build trimmed stripped binaries by default|local Go app Makefiles share the Linux binary build contract|local Go app Makefiles expose help for focused workflows|local Go app Makefiles expose clean for generated run artifacts|browser app js-check targets cover shipped vanilla web assets'`; `bats tests/app-layout-consistency.bats tests/apps-makefile.bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats tests/make-target-surfaces.bats`; `scripts/check-make-target-surfaces.sh --execute`; `make -n -C apps/subnetcalc/app build build-linux clean`; `make -n -C apps/platform-mcp/app build build-linux clean`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`. Full `bats tests/validate-docker-optimization-contracts.bats` still fails at existing unrelated test 44, `image catalog renders target tfvars external image projection`, because `validate-image-catalog-target-refs.sh` lacks `--print-expected`.
- Verdict: PRODUCTIVE

### Pass 16 - Behavioral image catalog projection contract - 2026-06-11T15:36:17Z

- Files changed: tests/app_contracts.py
- Changes: Removed the source-text assertion that required the shell wrapper to mention `--print-expected`; the target tfvars projection helper now treats the validator subprocess call with `--print-expected` as the public interface and keeps validating rendered platform/workload maps and expected Lima/Slicer refs.
- Red test: `bats tests/validate-docker-optimization-contracts.bats --filter 'image catalog renders target tfvars external image projection'` failed before implementation with `validate-image-catalog-target-refs.sh should expose --print-expected`.
- Verification: `bats tests/validate-docker-optimization-contracts.bats --filter 'image catalog renders target tfvars external image projection'`; `bats tests/validate-docker-optimization-contracts.bats --filter 'image catalog.*target|docker optimization tests share image catalog target'`; `bats tests/validate-docker-optimization-contracts.bats --print-output-on-failure`; `bats tests/app-layout-consistency.bats tests/apps-makefile.bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats tests/make-target-surfaces.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 17 - Shared Go module Make workflow - 2026-06-11T15:43:44Z

- Files changed: mk/shared-go-module.mk, apps/shared/apphttp/Makefile, apps/shared/appshell/Makefile, apps/shared/idpauth/Makefile, tests/app_contracts.py, tests/validate-docker-optimization-contracts.bats
- Changes: Added `mk/shared-go-module.mk` so shared Go modules use one implementation for help, test, and optional js-check behavior, while each module Makefile now declares its label and optional JS source list.
- Red test: `bats tests/validate-docker-optimization-contracts.bats --filter 'shared app module Makefiles share common Go module workflow'` failed before implementation because the shared module did not exist and the three shared module Makefiles duplicated help/test/js-check recipes.
- Verification: `bats tests/validate-docker-optimization-contracts.bats --filter 'shared app module Makefiles share common Go module workflow|shared app module Makefiles expose focused help|docker optimization tests share shared app module Makefile helpers'`; `make -C apps/shared/apphttp help`; `make -n -C apps/shared/appshell js-check`; `make -n -C apps/shared/idpauth js-check`; `make -C apps/shared/apphttp help test`; `bats tests/validate-docker-optimization-contracts.bats --print-output-on-failure` after serial rerun; `bats tests/app-layout-consistency.bats tests/apps-makefile.bats tests/sentiment-makefile.bats tests/subnetcalc-makefile.bats tests/make-target-surfaces.bats`; `scripts/check-make-target-surfaces.sh --execute`; `python3 -B -m py_compile tests/app_contracts.py`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 18 - Shared host-port listener probing - 2026-06-11T15:57:01Z

- Files changed: scripts/lib/host-port-listeners.sh, tests/host-port-listeners.bats, kubernetes/scripts/check-target-host-ports.sh, docker/compose/scripts/check-host-ports.sh
- Changes: Added a shared host-port listener library for lsof/ss probing, listener address projection, and bind-overlap checks, then routed the Kubernetes target and docker/compose host-port preflights through it while leaving command-specific Docker publisher ownership logic local.
- Red test: `bats tests/host-port-listeners.bats` initially failed because `scripts/lib/host-port-listeners.sh` did not exist.
- Verification: `bats tests/host-port-listeners.bats`; `bats -f 'kind ensure-kind-running fails before docker start when planned host ports are occupied|kind host port preflight' kubernetes/kind/tests/makefile.bats`; `bash -n scripts/lib/host-port-listeners.sh tests/host-port-listeners.bats kubernetes/scripts/check-target-host-ports.sh docker/compose/scripts/check-host-ports.sh`; stubbed `docker/compose/scripts/check-host-ports.sh --execute`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 19 - Slicer tfvars adapter usage - 2026-06-11T16:05:48Z

- Files changed: kubernetes/slicer/Makefile, kubernetes/slicer/tests/makefile.bats
- Changes: Routed Slicer `plan` and `apply` tfvars argument assembly through the shared `kubernetes/scripts/build-tfvar-args.sh` adapter, matching Lima's ordered optional-file contract for stage, target, `PLATFORM_BASE_TFVARS`, and `PLATFORM_TFVARS` while preserving kubeconfig, context, cluster, auto-approve, and feature-gate behavior.
- Red test: `bats kubernetes/slicer/tests/makefile.bats --filter 'slicer .*tfvar|slicer check-sso-e2e uses the split kubeconfig and rendered Backstage gate'` failed before implementation because Slicer still assembled `plan_args`, `apply_args`, and `tfvar_files` manually.
- Verification: `bats kubernetes/slicer/tests/makefile.bats --filter 'slicer .*tfvar|slicer check-sso-e2e uses the split kubeconfig and rendered Backstage gate'`; `bash -n kubernetes/scripts/build-tfvar-args.sh`; `git diff --check`. Full `bats kubernetes/slicer/tests/makefile.bats` still fails one pre-existing unrelated typo-suggestion test, `slicer typo suggests the closest workflow action`.
- Verdict: PRODUCTIVE

### Pass 20 - Local platform image target surface - 2026-06-11T16:09:56Z

- Files changed: kubernetes/lima/Makefile, kubernetes/lima/tests/makefile.bats, kubernetes/slicer/Makefile, kubernetes/slicer/tests/makefile.bats
- Changes: Declared the implemented and documented `build-local-platform-images` target in Lima and Slicer's `MAKE_KNOWN_GOALS`, restoring the public Make interface that their `apply` recipes already call and matching Kind's reachable target surface.
- Red test: `make -n -C kubernetes/lima build-local-platform-images` and `make -n -C kubernetes/slicer build-local-platform-images` failed before implementation with `Unknown make goal 'build-local-platform-images'` despite real `.PHONY` targets.
- Verification: `bats kubernetes/lima/tests/makefile.bats --filter 'build-local-platform-images|local platform image'`; `bats kubernetes/slicer/tests/makefile.bats --filter 'build-local-platform-images|local platform image|slicer .*tfvar'`; `make -n -C kubernetes/lima build-local-platform-images`; `make -n -C kubernetes/slicer build-local-platform-images`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 21 - Stage workflow suggestion guard handoff - 2026-06-11T16:13:37Z

- Files changed: mk/common.mk, kubernetes/kind/Makefile, kubernetes/lima/Makefile, kubernetes/slicer/Makefile
- Changes: Added an explicit `MAKE_UNKNOWN_GOAL_GUARD_DEFER_GOALS` interface so the shared common Make guard can defer unknown-goal handling when a valid stage goal is present, letting `mk/stage-workflow.mk` produce stage/action typo suggestions while preserving common's declared-goal rejection for undeclared public rules.
- Red test: `bats kubernetes/slicer/tests/makefile.bats --filter 'typo suggests'` failed before implementation because `mk/common.mk` stopped parsing on `aplly` before the stage workflow could suggest `apply`.
- Verification: `bats kubernetes/kind/tests/makefile.bats --filter 'typo suggests'`; `bats kubernetes/lima/tests/makefile.bats --filter 'typo suggests'`; `bats kubernetes/slicer/tests/makefile.bats --filter 'typo suggests'`; `bats tests/app-layout-consistency.bats --filter 'common app wrapper rejects public rules that are not declared goals|common app wrapper does not turn declared missing goals into empty successes'`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 22 - Slicer read-only tfvars adapter parity - 2026-06-11T16:20:59Z

- Files changed: kubernetes/slicer/Makefile, kubernetes/slicer/tests/makefile.bats
- Changes: Routed Slicer read-only check var-file assembly through the shared `BUILD_TFVAR_ARGS` adapter for `check-health`, `check-security`, `check-gateway-urls`, `check-app`, `check-sso`, `show-urls`, and the adjacent `check-host-ports` tfvars list, preserving stage-specified behavior and downstream command flags.
- Red test: `bats kubernetes/slicer/tests/makefile.bats --filter 'slicer read-only check targets delegate ordered optional tfvars to the shared adapter'` failed before implementation because those read-only targets still hand-built `vf` strings with local `PLATFORM_BASE_TFVARS` / `PLATFORM_TFVARS` checks.
- Verification: `bats kubernetes/slicer/tests/makefile.bats --filter 'slicer read-only check targets delegate ordered optional tfvars to the shared adapter'`; `bats kubernetes/slicer/tests/makefile.bats`; `make -n -C kubernetes/slicer 900 check-health PLATFORM_BASE_TFVARS=/tmp/base.tfvars PLATFORM_TFVARS=/tmp/operator.tfvars`; `make -n -C kubernetes/slicer 900 check-gateway-urls PLATFORM_BASE_TFVARS=/tmp/base.tfvars PLATFORM_TFVARS=/tmp/operator.tfvars`; `make -n -C kubernetes/slicer 900 check-app APP=grafana PLATFORM_BASE_TFVARS=/tmp/base.tfvars PLATFORM_TFVARS=/tmp/operator.tfvars`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 23 - Cross-runtime blocker listener diagnostics - 2026-06-11T16:26:53Z

- Files changed: kubernetes/scripts/check-lima-stopped.sh, kubernetes/scripts/check-slicer-stopped.sh, kubernetes/lima/tests/check-slicer-stopped.bats, kubernetes/slicer/tests/check-lima-stopped.bats
- Changes: Reused the shared `host-port-listeners.sh` adapter for Lima/Slicer cross-runtime blocker diagnostics so active shared bindings report normalized actual listener addresses instead of fabricated `127.0.0.1:<port>` entries.
- Red test: `bats kubernetes/slicer/tests/check-lima-stopped.bats` and `bats kubernetes/lima/tests/check-slicer-stopped.bats` failed before implementation on mixed listener binds because the scripts reported wildcard and non-loopback listeners as loopback addresses.
- Verification: `bats tests/host-port-listeners.bats`; `bats kubernetes/slicer/tests/check-lima-stopped.bats`; `bats kubernetes/lima/tests/check-slicer-stopped.bats`; `bash -n kubernetes/scripts/check-lima-stopped.sh kubernetes/scripts/check-slicer-stopped.sh`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 24 - Shared Slicer image-cache adapter - 2026-06-11T16:35:11Z

- Files changed: kubernetes/slicer/Makefile, kubernetes/slicer/tests/makefile.bats, sites/docs/content/reference/shell-scripts.mdx, kubernetes/slicer/scripts/ensure-image-cache.sh
- Changes: Routed Slicer `ensure-image-cache` through shared `kubernetes/scripts/ensure-local-image-cache.sh` while preserving Slicer locality with `CACHE_PUSH_HOST=127.0.0.1:5002` and `CACHE_CONTAINER_NAME=slicer-image-cache`, then removed the duplicated Slicer-local script and stale docs inventory row.
- Red test: `bats kubernetes/slicer/tests/makefile.bats --filter 'ensure-image-cache'` failed before implementation because the Make target still delegated to `kubernetes/slicer/scripts/ensure-image-cache.sh`.
- Verification: `bats kubernetes/slicer/tests/makefile.bats --filter 'ensure-image-cache'`; `bats kubernetes/slicer/tests/makefile.bats`; `bats tests/audit-shell-scripts.bats --filter 'slicer|shell audit'`; `bats tests/docs-content-current.bats`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 25 - Shared SSO E2E env projection - 2026-06-11T16:45:10Z

- Files changed: kubernetes/scripts/build-sso-e2e-env.sh, tests/sso-e2e-env.bats, kubernetes/kind/Makefile, kubernetes/lima/Makefile, kubernetes/slicer/Makefile, kubernetes/kind/tests/makefile.bats, kubernetes/lima/tests/makefile.bats, kubernetes/slicer/tests/makefile.bats
- Changes: Added a shared Kubernetes SSO E2E environment adapter that builds shell assignments for `SSO_E2E_ENABLE_BACKSTAGE`, `STAGE_TFVARS`, and `STAGE_TFVARS_FILES` from an ordered tfvars chain, then routed Kind, Lima, and Slicer `check-sso-e2e` recipes through it while preserving each runtime's local preflight checks.
- Red test: `bats tests/sso-e2e-env.bats` failed before implementation because `kubernetes/scripts/build-sso-e2e-env.sh` did not exist.
- Verification: `bats tests/sso-e2e-env.bats`; `bash -n kubernetes/scripts/build-sso-e2e-env.sh`; `shellcheck kubernetes/scripts/build-sso-e2e-env.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/build-sso-e2e-env.sh`; `bats kubernetes/kind/tests/makefile.bats`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `bats tests/kubernetes-mcp-manifests.bats --filter 'SSO|MCP|mcp'`; `scripts/check-make-target-surfaces.sh --execute`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `git diff --check`. Accidental recursive `make -n -C kubernetes/kind check-sso-e2e STAGE=900` executed the live browser SSO suite and passed with 20 passed / 1 skipped; analogous Lima/Slicer dry-run probes crossed into live runtime checks and failed on environment/runtime state, so they are not counted as code verification.
- Verdict: PRODUCTIVE

### Pass 26 - Shared Kubernetes launchpad render adapter - 2026-06-11T16:53:56Z

- Files changed: kubernetes/scripts/render-launchpad.sh, tests/kubernetes-launchpad-render.bats, kubernetes/kind/Makefile, kubernetes/lima/Makefile, kubernetes/slicer/Makefile
- Changes: Added a shared Kubernetes `launchpad-render` adapter around `terraform/kubernetes/scripts/render-platform-launchpad.sh`, then routed Kind, Lima, and Slicer `launchpad-render` targets through the shared adapter while keeping runtime Makefiles responsible only for their stack path and dry-run/execute mode.
- Red test: `bats tests/kubernetes-launchpad-render.bats` failed before implementation because the runtime Makefiles still rendered launchpad dashboards with duplicated inline shell blocks instead of delegating to `kubernetes/scripts/render-launchpad.sh`.
- Verification: `bats tests/kubernetes-launchpad-render.bats`; `bash -n kubernetes/scripts/render-launchpad.sh`; `shellcheck kubernetes/scripts/render-launchpad.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/render-launchpad.sh`; `make -C kubernetes/kind launchpad-render DRY_RUN=1`; `make -C kubernetes/lima launchpad-render DRY_RUN=1`; `make -C kubernetes/slicer launchpad-render DRY_RUN=1`; `scripts/check-make-target-surfaces.sh --execute`; `bats tests/make-target-surfaces.bats --filter 'target surface|make known goals'`; `bats kubernetes/kind/tests/makefile.bats`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 27 - Shared image-cache preload env projection - 2026-06-11T17:01:05Z

- Files changed: kubernetes/scripts/build-image-cache-env.sh, tests/image-cache-env.bats, kubernetes/kind/Makefile, kubernetes/lima/Makefile, kubernetes/slicer/Makefile
- Changes: Added a shared Kubernetes image-cache environment adapter that emits shell assignments for the `PRELOAD_ENABLE_*` gates from an ordered tfvars chain, then routed Kind, Lima, and Slicer `sync-image-cache` targets through it while preserving runtime-local cache hosts, image lists, optional mode, and Kind operator overrides.
- Red test: `bats tests/image-cache-env.bats` failed before implementation because `kubernetes/scripts/build-image-cache-env.sh` did not exist.
- Verification: `bats tests/image-cache-env.bats`; `bash -n kubernetes/scripts/build-image-cache-env.sh`; `shellcheck kubernetes/scripts/build-image-cache-env.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/build-image-cache-env.sh`; `make -n -C kubernetes/kind sync-image-cache STAGE=900 DRY_RUN=1`; `make -n -C kubernetes/lima sync-image-cache STAGE=900 DRY_RUN=1`; `make -n -C kubernetes/slicer sync-image-cache STAGE=900 DRY_RUN=1`; `scripts/check-make-target-surfaces.sh --execute`; `bats kubernetes/kind/tests/makefile.bats`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 28 - Direct stage monotonicity adapter usage - 2026-06-11T17:08:31Z

- Files changed: kubernetes/lima/Makefile, kubernetes/slicer/Makefile, tests/kubernetes-stage-monotonicity-adapter.bats, sites/docs/content/reference/shell-scripts.mdx, kubernetes/lima/scripts/check-stage-monotonicity.sh, kubernetes/slicer/scripts/check-stage-monotonicity.sh
- Changes: Removed Lima/Slicer pure forwarding stage-monotonicity scripts and had their Make targets call shared `kubernetes/scripts/check-stage-monotonicity.sh` directly with runtime-specific `--stack-dir`, `--stages-dir`, and `--label` arguments; docs now list the shared checker only.
- Red test: `bats tests/kubernetes-stage-monotonicity-adapter.bats` failed before implementation because Lima/Slicer still delegated through runtime-local wrapper scripts.
- Verification: `bats tests/kubernetes-stage-monotonicity-adapter.bats`; `make -C kubernetes/lima check-stage-monotonicity`; `make -C kubernetes/slicer check-stage-monotonicity`; `bats tests/kubernetes-stage-monotonicity-adapter.bats kubernetes/kind/tests/makefile.bats --filter 'stage monotonicity'`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `bats tests/audit-shell-scripts.bats --filter 'shell audit|stage-monotonicity|script'`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `scripts/check-make-target-surfaces.sh --execute`; `bash -n kubernetes/scripts/check-stage-monotonicity.sh`; `git diff --check`; no stale references from `rg 'kubernetes/(lima|slicer)/scripts/check-stage-monotonicity|lima/scripts/check-stage-monotonicity|slicer/scripts/check-stage-monotonicity' .`.
- Verdict: PRODUCTIVE

### Pass 29 - Kind direct stage monotonicity adapter usage - 2026-06-11T17:11:31Z

- Files changed: kubernetes/kind/Makefile, tests/kubernetes-stage-monotonicity-adapter.bats, sites/docs/content/reference/shell-scripts.mdx, kubernetes/kind/scripts/check-stage-monotonicity.sh
- Changes: Extended the stage-monotonicity adapter contract to Kind, routed `make -C kubernetes/kind check-stage-monotonicity` directly through shared `kubernetes/scripts/check-stage-monotonicity.sh` with Kind's stack label, removed the final runtime-local forwarding wrapper, and removed the stale Kind wrapper from the shell script inventory.
- Red test: `bats tests/kubernetes-stage-monotonicity-adapter.bats` failed before implementation because Kind still printed `kubernetes/kind/scripts/check-stage-monotonicity.sh` instead of the shared checker.
- Verification: `bats tests/kubernetes-stage-monotonicity-adapter.bats`; `make -C kubernetes/kind check-stage-monotonicity`; `bats tests/kubernetes-stage-monotonicity-adapter.bats kubernetes/kind/tests/makefile.bats --filter 'stage monotonicity'`; `bats kubernetes/kind/tests/makefile.bats`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `bats tests/audit-shell-scripts.bats --filter 'shell audit|stage-monotonicity|script'`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`; no stale references from `rg 'kubernetes/(kind|lima|slicer)/scripts/check-stage-monotonicity|kind/scripts/check-stage-monotonicity|lima/scripts/check-stage-monotonicity|slicer/scripts/check-stage-monotonicity' .`.
- Verdict: PRODUCTIVE

### Pass 30 - Direct host gateway proxy adapter usage - 2026-06-11T17:15:34Z

- Files changed: kubernetes/lima/Makefile, kubernetes/slicer/Makefile, tests/kubernetes-host-gateway-proxy-adapter.bats, sites/docs/content/reference/shell-scripts.mdx, docs/plans/architecture-deepening-candidates.md, kubernetes/lima/scripts/host-gateway-proxy.sh, kubernetes/slicer/scripts/host-gateway-proxy.sh
- Changes: Removed Lima/Slicer pure forwarding host-gateway proxy scripts and had their Make targets pass runtime labels, container names, image tags, ports, and explicit `--action` values directly into shared `kubernetes/scripts/host-gateway-proxy.sh`; docs now list the shared proxy script only and planning references point at that shared boundary.
- Red test: `bats tests/kubernetes-host-gateway-proxy-adapter.bats` failed before implementation because Lima/Slicer targets delegated through runtime-local wrapper scripts instead of configuring the shared proxy adapter directly.
- Verification: `bats tests/kubernetes-host-gateway-proxy-adapter.bats kubernetes/tests/host-gateway-proxy.bats`; `bats kubernetes/lima/tests/makefile.bats --filter 'host-gateway|proxy|stop-lima'`; `bats kubernetes/slicer/tests/makefile.bats --filter 'host-gateway|proxy|stop-slicer|host-forward'`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `bats tests/audit-shell-scripts.bats --filter 'shell audit|script'`; `scripts/check-make-target-surfaces.sh --execute`; `bash -n kubernetes/scripts/host-gateway-proxy.sh`; `git diff --check`; no stale deleted-wrapper references from `rg 'kubernetes/(lima|slicer)/scripts/host-gateway-proxy|lima/scripts/host-gateway-proxy|slicer/scripts/host-gateway-proxy' . --glob '!tests/kubernetes-host-gateway-proxy-adapter.bats'`.
- Verdict: PRODUCTIVE

### Pass 31 - Direct image cache sync adapter usage - 2026-06-11T17:19:20Z

- Files changed: kubernetes/lima/Makefile, kubernetes/slicer/Makefile, tests/kubernetes-sync-image-cache-adapter.bats, kubernetes/tests/sync-local-image-cache.bats, sites/docs/content/reference/shell-scripts.mdx, kubernetes/lima/scripts/sync-local-image-cache.sh, kubernetes/slicer/scripts/sync-local-image-cache.sh, kubernetes/lima/tests/sync-local-image-cache.bats, kubernetes/slicer/tests/sync-local-image-cache.bats
- Changes: Removed Lima/Slicer pure forwarding image-cache sync scripts and had their Make targets pass `VARIANT_LABEL`, runtime image-list paths, cache hosts, optional mode, and preload gates directly into shared `kubernetes/scripts/sync-local-image-cache.sh`; duplicate wrapper-path behavior tests collapsed into one shared script test and docs now list only the shared sync script.
- Red test: `bats tests/kubernetes-sync-image-cache-adapter.bats` failed before implementation because Lima/Slicer `sync-image-cache` targets still delegated through runtime-local wrapper scripts instead of configuring the shared image sync adapter directly.
- Verification: `bats tests/kubernetes-sync-image-cache-adapter.bats kubernetes/tests/sync-local-image-cache.bats`; `bats tests/image-cache-env.bats`; `bats kubernetes/lima/tests/makefile.bats --filter 'sync-image-cache|image cache|target profile'`; `bats kubernetes/slicer/tests/makefile.bats --filter 'sync-image-cache|image cache|target profile'`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `bats tests/audit-shell-scripts.bats --filter 'shell audit|script|sync-local-image-cache'`; `scripts/check-make-target-surfaces.sh --execute`; `bash -n kubernetes/scripts/sync-local-image-cache.sh`; `git diff --check`; no stale deleted-wrapper references from `rg 'kubernetes/(lima|slicer)/scripts/sync-local-image-cache|lima/scripts/sync-local-image-cache|slicer/scripts/sync-local-image-cache' . --glob '!tests/kubernetes-sync-image-cache-adapter.bats'`.
- Verdict: PRODUCTIVE

### Pass 32 - Slicer shared stack prereqs adapter - 2026-06-11T17:21:35Z

- Files changed: kubernetes/slicer/Makefile, kubernetes/slicer/tests/makefile.bats
- Changes: Routed Slicer's common host tool inventory through shared `kubernetes/scripts/check-stack-prereqs.sh`, matching Kind/Lima's required, optional, and stage-aware recommended-tool behavior while keeping Slicer-specific local endpoint validation, bootstrap-client fallback, Docker daemon checks, daemon checks, versions, platform env, and kubeconfig validation local.
- Red test: `bats kubernetes/slicer/tests/makefile.bats --filter 'prereqs'` failed before implementation because Slicer had no `CHECK_STACK_PREREQS` delegation and still carried inline `check_bin` / missing-tool machinery.
- Verification: `bats kubernetes/slicer/tests/makefile.bats --filter 'prereqs'`; `bats kubernetes/kind/tests/makefile.bats --filter 'prereqs|stack prereqs'`; `bats kubernetes/lima/tests/makefile.bats --filter 'prereqs'`; `bats kubernetes/slicer/tests/makefile.bats`; `bash -n kubernetes/scripts/check-stack-prereqs.sh`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 33 - Direct workload image builder adapter usage - 2026-06-11T17:27:20Z

- Files changed: kubernetes/lima/Makefile, kubernetes/slicer/Makefile, tests/kubernetes-workload-image-builder-adapter.bats, sites/docs/content/reference/shell-scripts.mdx, tests/app_contracts.py, scripts/trivy-scan-apps.sh, kubernetes/lima/scripts/build-local-workload-images.sh, kubernetes/slicer/scripts/build-local-workload-images.sh
- Changes: Removed Lima/Slicer pure forwarding workload-image builder scripts and had their Make targets pass `VARIANT_LABEL`, cache push host, image namespace, and tag directly into shared `kubernetes/scripts/build-local-workload-images.sh`; docs and contract helpers now treat the shared workload builder as the public implementation seam.
- Red test: `bats tests/kubernetes-workload-image-builder-adapter.bats` failed before implementation because Lima/Slicer `build-workload-images` dry-runs did not expose runtime labels and still delegated through runtime-local wrapper scripts.
- Verification: `bats tests/kubernetes-workload-image-builder-adapter.bats`; `make -n -C kubernetes/lima build-workload-images`; `make -n -C kubernetes/slicer build-workload-images`; `bats kubernetes/lima/tests/makefile.bats --filter 'build-workload|workload image|local workload|build-local'`; `bats kubernetes/slicer/tests/makefile.bats --filter 'build-workload|workload image|local workload|build-local'`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `bats tests/audit-shell-scripts.bats --filter 'shell audit|script|build-local-workload'`; `bats tests/validate-docker-optimization-contracts.bats --filter 'image builder|workload image|prebuild|catalog build|docker optimization'`; `python3 -B -m py_compile tests/app_contracts.py`; `scripts/check-make-target-surfaces.sh --execute`; `bash -n kubernetes/scripts/build-local-workload-images.sh`; `git diff --check`; no stale deleted-wrapper references from `rg 'kubernetes/(lima|slicer)/scripts/build-local-workload-images|lima/scripts/build-local-workload-images|slicer/scripts/build-local-workload-images' . --glob '!tests/kubernetes-workload-image-builder-adapter.bats'`.
- Verdict: PRODUCTIVE

### Pass 34 - Direct Kind-stopped checker usage - 2026-06-11T17:32:11Z

- Files changed: kubernetes/lima/Makefile, kubernetes/slicer/Makefile, tests/kubernetes-kind-stopped-adapter.bats, kubernetes/lima/tests/check-kind-stopped.bats, kubernetes/slicer/tests/check-kind-stopped.bats, sites/docs/content/reference/shell-scripts.mdx, kubernetes/lima/scripts/check-kind-stopped.sh, kubernetes/slicer/scripts/check-kind-stopped.sh
- Changes: Removed Lima/Slicer pure forwarding Kind-stopped scripts and had their `check-kind-stopped` Make targets call shared `kubernetes/scripts/check-kind-stopped.sh` directly; existing blocker behavior tests now exercise the shared checker and docs list only the shared implementation.
- Red test: `bats tests/kubernetes-kind-stopped-adapter.bats` failed before implementation because Lima dry-run output still delegated through `kubernetes/lima/scripts/check-kind-stopped.sh`.
- Verification: `bats tests/kubernetes-kind-stopped-adapter.bats`; `bats kubernetes/lima/tests/check-kind-stopped.bats kubernetes/slicer/tests/check-kind-stopped.bats`; `make -n -C kubernetes/lima check-kind-stopped && make -n -C kubernetes/slicer check-kind-stopped`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `bats tests/audit-shell-scripts.bats --filter 'shell audit|script|check-kind-stopped'`; `bash -n kubernetes/scripts/check-kind-stopped.sh`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`; no stale deleted-wrapper references from `rg 'kubernetes/(lima|slicer)/scripts/check-kind-stopped|lima/scripts/check-kind-stopped|slicer/scripts/check-kind-stopped' . --glob '!tests/kubernetes-kind-stopped-adapter.bats'`.
- Verdict: PRODUCTIVE

### Pass 35 - Direct host port preflight adapter usage - 2026-06-11T17:40:34Z

- Files changed: kubernetes/kind/Makefile, kubernetes/slicer/Makefile, tests/kubernetes-host-port-preflight-adapter.bats, sites/docs/content/reference/shell-scripts.mdx, docs/iac-boundaries.md, kubernetes/kind/README.md, docs/plans/architecture-deepening-candidates.md, kubernetes/kind/scripts/check-kind-host-ports.sh, kubernetes/slicer/scripts/check-slicer-host-ports.sh
- Changes: Removed Kind/Slicer host-port preflight wrapper scripts and had their public Make targets configure shared `kubernetes/scripts/check-target-host-ports.sh` directly with runtime labels, ordered tfvars, gateway/admin port inventories, and tfvars-driven admin-port gates; docs now point at the shared checker as the public host-port seam.
- Red test: `bats tests/kubernetes-host-port-preflight-adapter.bats` failed before implementation because Kind still expanded through `kubernetes/kind/scripts/check-kind-host-ports.sh`.
- Verification: `bats tests/kubernetes-host-port-preflight-adapter.bats kubernetes/kind/tests/makefile.bats --filter 'host port|host ports|check-kind-host-ports'`; `bats kubernetes/slicer/tests/makefile.bats --filter 'host port|host ports|check-host-ports|preflight'`; `bats kubernetes/slicer/tests/makefile.bats`; `bats kubernetes/kind/tests/makefile.bats`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `bats tests/audit-shell-scripts.bats --filter 'shell audit|script|host-ports|check-target-host-ports'`; `make -n -C kubernetes/kind check-kind-host-ports STAGE=100`; `make -n -C kubernetes/slicer check-host-ports STAGE=100`; `scripts/check-make-target-surfaces.sh --execute`; `bash -n kubernetes/scripts/check-target-host-ports.sh`; `git diff --check`; no stale deleted-wrapper references from `rg 'kubernetes/(kind/scripts/check-kind-host-ports|slicer/scripts/check-slicer-host-ports)|check-kind-host-ports\.sh|check-slicer-host-ports\.sh' . --glob '!tests/kubernetes-host-port-preflight-adapter.bats'`.
- Verdict: PRODUCTIVE

### Pass 36 - Variant-declared blocker dispatch - 2026-06-11T17:47:58Z

- Files changed: kubernetes/kind/Makefile, kubernetes/lima/Makefile, kubernetes/slicer/Makefile, kubernetes/scripts/variant-blocker-checks.sh, tests/kubernetes-variant-blocker-dispatch.bats
- Changes: Added a shared blocker-check adapter that reads ordered `.blockers.checks[]` targets from each Kubernetes variant manifest, then routed Kind/Lima/Slicer `check-conflicting-clusters-stopped` through those manifest-declared orders while preserving the existing individual check targets and order.
- Red test: `bats tests/kubernetes-variant-blocker-dispatch.bats` failed before implementation because the aggregate recipes still hardcoded recursive Make calls and did not reference variant manifests.
- Verification: `bats tests/kubernetes-variant-blocker-dispatch.bats`; `make -n -C kubernetes/kind check-conflicting-clusters-stopped`; `make -n -C kubernetes/lima check-conflicting-clusters-stopped`; `make -n -C kubernetes/slicer check-conflicting-clusters-stopped`; `kubernetes/scripts/variant-blocker-checks.sh --execute --variant-json kubernetes/variants/kind/variant.json`; `kubernetes/scripts/variant-blocker-checks.sh --execute --variant-json kubernetes/variants/lima/variant.json`; `kubernetes/scripts/variant-blocker-checks.sh --execute --variant-json kubernetes/variants/slicer/variant.json`; `bash -n kubernetes/scripts/variant-blocker-checks.sh`; `scripts/check-bash32-compat.sh --execute kubernetes/scripts/variant-blocker-checks.sh`.
- Verdict: PRODUCTIVE

### Pass 37 - Variant-declared host port preflight - 2026-06-11T18:03:21Z

- Files changed: kubernetes/kind/Makefile, kubernetes/slicer/Makefile, kubernetes/scripts/check-target-host-ports.sh, tests/kubernetes-host-port-preflight-adapter.bats
- Changes: Extended the shared host-port checker with `--variant-json` so gateway and admin preflight rows are derived from variant host-access facts plus ordered tfvars, then routed Kind/Slicer preflight Make targets through that adapter without inline `PORT_CHECKS` inventory construction.
- Red test: `bats tests/kubernetes-host-port-preflight-adapter.bats` failed before implementation because the Make dry-runs still exported inline `PORT_CHECKS` and the script did not accept `--variant-json`.
- Verification: `bats tests/kubernetes-host-port-preflight-adapter.bats`; `bats kubernetes/kind/tests/makefile.bats --filter 'kind host port preflight'`; `bats kubernetes/slicer/tests/makefile.bats --filter 'host port|host ports|check-host-ports|preflight'`; `bats tests/host-access-contracts.bats`; `bats kubernetes/kind/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `make -n -C kubernetes/kind check-kind-host-ports STAGE=100`; `make -n -C kubernetes/slicer check-host-ports STAGE=100`; `bash -n kubernetes/scripts/check-target-host-ports.sh`; `/bin/bash -n kubernetes/scripts/check-target-host-ports.sh`; `make lint-bash32`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 38 - Kind direct workload image builder adapter usage - 2026-06-11T18:07:29Z

- Files changed: kubernetes/kind/Makefile, tests/kubernetes-workload-image-builder-adapter.bats, tests/app_contracts.py, sites/docs/content/reference/shell-scripts.mdx, kubernetes/kind/scripts/build-local-workload-images.sh
- Changes: Removed the Kind-local workload-image builder implementation and routed `make -C kubernetes/kind build-local-workload-images` directly through shared `kubernetes/scripts/build-local-workload-images.sh` with `VARIANT_LABEL`, cache host, namespace, and tag inputs, matching Lima/Slicer's shared adapter boundary.
- Red test: `bats tests/kubernetes-workload-image-builder-adapter.bats` failed before implementation because Kind did not pass `VARIANT_LABEL="Kind"` and still used `kubernetes/kind/scripts/build-local-workload-images.sh`.
- Verification: `bats tests/kubernetes-workload-image-builder-adapter.bats`; `make -n -C kubernetes/kind build-local-workload-images`; `bats tests/validate-docker-optimization-contracts.bats --filter 'image builder|workload image|prebuild|catalog build|docker optimization'`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `bats tests/audit-shell-scripts.bats --filter 'shell audit|script|build-local-workload'`; `bats kubernetes/kind/tests/makefile.bats`; `bats tests/app-layout-consistency.bats --filter 'image|script|kind|workload|shell'`; `python3 -B -m py_compile tests/app_contracts.py`; `bash -n kubernetes/scripts/build-local-workload-images.sh`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`; no stale Kind-local workload builder references outside the adapter test negative assertion.
- Verdict: PRODUCTIVE

### Pass 39 - Shared platform image builder boundary - 2026-06-11T18:16:21Z

- Files changed: kubernetes/scripts/build-local-platform-images.sh, kubernetes/kind/scripts/build-local-platform-images.sh, kubernetes/kind/Makefile, kubernetes/lima/Makefile, kubernetes/slicer/Makefile, tests/kubernetes-platform-image-builder-adapter.bats, kubernetes/lima/tests/makefile.bats, kubernetes/slicer/tests/makefile.bats, kubernetes/kind/tests/makefile.bats, tests/app_contracts.py, tests/backstage-portal.bats, sites/docs/content/reference/shell-scripts.mdx, skills/use-platform/SKILL.md
- Changes: Moved the platform image builder out of Kind-owned scripts into shared `kubernetes/scripts/build-local-platform-images.sh`, routed Kind/Lima/Slicer `build-local-platform-images` through that shared path, and updated path-based contracts, docs, and repo guidance to treat it as the shared platform-image boundary.
- Red test: `bats tests/kubernetes-platform-image-builder-adapter.bats` failed before implementation because all three runtime dry-runs still referenced `kubernetes/kind/scripts/build-local-platform-images.sh`.
- Verification: `bats tests/kubernetes-platform-image-builder-adapter.bats`; `bats tests/validate-docker-optimization-contracts.bats --filter 'image builder|platform image|platform cache|source fingerprint|Grafana plugin|catalog build|docker optimization'`; `bats tests/backstage-portal.bats`; `bats kubernetes/kind/tests/makefile.bats`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `make -n -C kubernetes/kind build-local-platform-images`; `make -n -C kubernetes/lima build-local-platform-images`; `make -n -C kubernetes/slicer build-local-platform-images`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `bats tests/audit-shell-scripts.bats --filter 'shell audit|script|build-local-platform'`; `bash -n kubernetes/scripts/build-local-platform-images.sh`; `python3 -B -m py_compile tests/app_contracts.py`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`; no stale Kind-local platform builder references outside the adapter test negative assertion.
- Verdict: PRODUCTIVE

### Pass 40 - Shared local image cache library boundary - 2026-06-11T18:20:14Z

- Files changed: kubernetes/scripts/local-cache-lib.sh, kubernetes/kind/scripts/local-cache-lib.sh, kubernetes/scripts/build-local-platform-images.sh, tests/kubernetes-platform-image-builder-adapter.bats, sites/docs/content/reference/shell-scripts.mdx
- Changes: Moved generic local registry cache helpers out of Kind-owned scripts into shared `kubernetes/scripts/local-cache-lib.sh`, then updated the shared platform image builder and shell script inventory to use the shared helper path.
- Red test: `bats tests/kubernetes-platform-image-builder-adapter.bats` failed before implementation because `kubernetes/scripts/build-local-platform-images.sh` still sourced `kubernetes/kind/scripts/local-cache-lib.sh`.
- Verification: `bats tests/kubernetes-platform-image-builder-adapter.bats`; `bats tests/validate-docker-optimization-contracts.bats --filter 'image builder|platform image|platform cache|source fingerprint|Grafana plugin|catalog build|docker optimization'`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `bats tests/audit-shell-scripts.bats --filter 'shell audit|script|local-cache|build-local-platform'`; `bash -n kubernetes/scripts/local-cache-lib.sh kubernetes/scripts/build-local-platform-images.sh`; `python3 -B -m py_compile tests/app_contracts.py`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`; no stale Kind-local cache helper references outside the adapter test negative assertion.
- Verdict: PRODUCTIVE

### Pass 41 - Shared k3s apiserver OIDC configurator - 2026-06-11T18:28:00Z

- Files changed: kubernetes/scripts/configure-k3s-apiserver-oidc.sh, kubernetes/lima/scripts/configure-k3s-apiserver-oidc.sh, kubernetes/slicer/scripts/configure-k3s-apiserver-oidc.sh, kubernetes/lima/tests/configure-k3s-apiserver-oidc.bats, kubernetes/slicer/tests/configure-k3s-apiserver-oidc.bats, sites/docs/content/reference/shell-scripts.mdx
- Changes: Moved duplicated Lima/Slicer k3s apiserver OIDC behavior into the shared configurator while keeping public runtime scripts as thin adapters that pass the selected transport/runtime config.
- Red test: `bats kubernetes/lima/tests/configure-k3s-apiserver-oidc.bats` and `bats kubernetes/slicer/tests/configure-k3s-apiserver-oidc.bats` failed before implementation because the wrappers did not delegate to a shared script and the shared script did not exist.
- Verification: `bats kubernetes/lima/tests/configure-k3s-apiserver-oidc.bats kubernetes/slicer/tests/configure-k3s-apiserver-oidc.bats`; `bats kubernetes/lima/tests/makefile.bats --filter 'configure-k3s-apiserver-oidc|stage 900 apply wires k3s apiserver OIDC|stage 900 apply waits for cluster health after k3s apiserver OIDC|check-sso-e2e does not repair k3s apiserver OIDC'`; `bats kubernetes/slicer/tests/makefile.bats --filter 'configure-k3s-apiserver-oidc|stage 900 apply wires k3s apiserver OIDC|stage 900 apply waits for cluster health after k3s apiserver OIDC|check-sso-e2e does not repair k3s apiserver OIDC'`; `bash -n kubernetes/scripts/configure-k3s-apiserver-oidc.sh kubernetes/lima/scripts/configure-k3s-apiserver-oidc.sh kubernetes/slicer/scripts/configure-k3s-apiserver-oidc.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/configure-k3s-apiserver-oidc.sh --path kubernetes/lima/scripts/configure-k3s-apiserver-oidc.sh --path kubernetes/slicer/scripts/configure-k3s-apiserver-oidc.sh`; `kubernetes/scripts/configure-k3s-apiserver-oidc.sh --shell-entrypoint-descriptor`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `scripts/audit-shell-scripts.sh --execute --path kubernetes/scripts/configure-k3s-apiserver-oidc.sh --path kubernetes/lima/scripts/configure-k3s-apiserver-oidc.sh --path kubernetes/slicer/scripts/configure-k3s-apiserver-oidc.sh`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 42 - Shared Lima SSH tunnel lifecycle - 2026-06-11T18:39:08Z

- Files changed: kubernetes/scripts/ssh-tunnel-lib.sh, kubernetes/lima/scripts/ensure-k3s-api-tunnel.sh, kubernetes/lima/scripts/ensure-k3s-shared-port-tunnels.sh, kubernetes/lima/tests/ssh-agent.bats, sites/docs/content/reference/shell-scripts.mdx
- Changes: Extracted the duplicated Lima SSH tunnel PID cleanup, config check, `ssh -F` startup, and readiness wait loop into `kubernetes/scripts/ssh-tunnel-lib.sh`, leaving the API tunnel and shared-port tunnel entrypoints to own only their readiness probes and forwarding parameters.
- Red test: `bats kubernetes/lima/tests/ssh-agent.bats --filter 'Lima tunnel entrypoints delegate SSH tunnel lifecycle'` failed before implementation because the shared helper did not exist and both scripts still owned the raw SSH lifecycle.
- Verification: `bats kubernetes/lima/tests/ssh-agent.bats --filter 'ensure-k3s-api-tunnel|Lima tunnel entrypoints'`; `bats kubernetes/lima/tests/ssh-agent.bats`; `bats kubernetes/lima/tests/makefile.bats --filter 'ensure-k3s-api-tunnel|shared-port|shared port|stage 900|check-sso-e2e'`; `make -n -C kubernetes/lima ensure-k3s-api-tunnel`; `make -n -C kubernetes/lima ensure-k3s-shared-port-tunnels`; `bash -n kubernetes/scripts/ssh-tunnel-lib.sh kubernetes/lima/scripts/ensure-k3s-api-tunnel.sh kubernetes/lima/scripts/ensure-k3s-shared-port-tunnels.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/ssh-tunnel-lib.sh --path kubernetes/lima/scripts/ensure-k3s-api-tunnel.sh --path kubernetes/lima/scripts/ensure-k3s-shared-port-tunnels.sh`; `shellcheck kubernetes/scripts/ssh-tunnel-lib.sh kubernetes/lima/scripts/ensure-k3s-api-tunnel.sh kubernetes/lima/scripts/ensure-k3s-shared-port-tunnels.sh`; `scripts/audit-shell-scripts.sh --execute --path kubernetes/scripts/ssh-tunnel-lib.sh --path kubernetes/lima/scripts/ensure-k3s-api-tunnel.sh --path kubernetes/lima/scripts/ensure-k3s-shared-port-tunnels.sh`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 43 - Shared k3s registry mirror renderer - 2026-06-11T18:45:53Z

- Files changed: kubernetes/scripts/k3s-registries-lib.sh, kubernetes/tests/k3s-registries-lib.bats, kubernetes/lima/scripts/bootstrap-k3s-lima.sh, kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh, sites/docs/content/reference/shell-scripts.mdx
- Changes: Extracted duplicated k3s registry mirror parsing and `registries.yaml` payload rendering from the Lima and Slicer bootstrap scripts into `kubernetes/scripts/k3s-registries-lib.sh`, leaving each runtime bootstrap adapter to own only remote copy, comparison, and restart behavior.
- Red test: `bats kubernetes/tests/k3s-registries-lib.bats` failed before implementation because the shared renderer did not exist.
- Verification: `bats kubernetes/tests/k3s-registries-lib.bats`; `bats kubernetes/slicer/tests/bootstrap-k3s-slicer.bats`; `bats kubernetes/lima/tests/ssh-agent.bats`; `bash -n kubernetes/scripts/k3s-registries-lib.sh kubernetes/lima/scripts/bootstrap-k3s-lima.sh kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/k3s-registries-lib.sh --path kubernetes/lima/scripts/bootstrap-k3s-lima.sh --path kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`; `shellcheck kubernetes/scripts/k3s-registries-lib.sh kubernetes/lima/scripts/bootstrap-k3s-lima.sh kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`; `scripts/audit-shell-scripts.sh --execute --path kubernetes/scripts/k3s-registries-lib.sh --path kubernetes/lima/scripts/bootstrap-k3s-lima.sh --path kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`; no stale bootstrap-local registry renderer functions from `rg 'registry_for_image_ref|default_endpoint_for_registry|platform_mirror_registries|append_mirror_entry' kubernetes/lima/scripts/bootstrap-k3s-lima.sh kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`.
- Verdict: PRODUCTIVE

### Pass 44 - Shared k3sup bootstrap support - 2026-06-11T18:53:45Z

- Files changed: kubernetes/scripts/k3s-bootstrap-lib.sh, kubernetes/tests/k3s-bootstrap-lib.bats, kubernetes/lima/scripts/bootstrap-k3s-lima.sh, kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh, sites/docs/content/reference/shell-scripts.mdx
- Changes: Extracted duplicated host-side k3sup client discovery, k3s channel/version argument rendering, and macOS-compatible timeout execution into `kubernetes/scripts/k3s-bootstrap-lib.sh`, while leaving Lima/Slicer VM transport, SSH key setup, install retries, kubeconfig handling, and runtime bootstrap policy local.
- Red test: `bats kubernetes/tests/k3s-bootstrap-lib.bats` failed before implementation because the shared bootstrap helper did not exist.
- Verification: `bats kubernetes/tests/k3s-bootstrap-lib.bats`; `bats kubernetes/lima/tests/ssh-agent.bats`; `bats kubernetes/slicer/tests/bootstrap-k3s-slicer.bats`; `bash -n kubernetes/scripts/k3s-bootstrap-lib.sh kubernetes/lima/scripts/bootstrap-k3s-lima.sh kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/k3s-bootstrap-lib.sh --path kubernetes/lima/scripts/bootstrap-k3s-lima.sh --path kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`; `shellcheck kubernetes/scripts/k3s-bootstrap-lib.sh kubernetes/lima/scripts/bootstrap-k3s-lima.sh kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`; `scripts/audit-shell-scripts.sh --execute --path kubernetes/scripts/k3s-bootstrap-lib.sh --path kubernetes/lima/scripts/bootstrap-k3s-lima.sh --path kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`; no stale runtime-local bootstrap support functions from `rg 'find_bootstrap_client\(|run_with_timeout\(|channel_args=\(--k3s' kubernetes/lima/scripts/bootstrap-k3s-lima.sh kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`.
- Verdict: PRODUCTIVE

### Pass 45 - Shared split-kubeconfig reconciliation adapter - 2026-06-11T19:01:47Z

- Files changed: kubernetes/scripts/reconcile-kubeconfig.sh, tests/kubernetes-kubeconfig-reconcile-adapter.bats, kubernetes/kind/scripts/ensure-kind-kubeconfig.sh, kubernetes/lima/scripts/bootstrap-k3s-lima.sh, kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh, sites/docs/content/reference/shell-scripts.mdx
- Changes: Extracted the default-kubeconfig merge/delete policy into `kubernetes/scripts/reconcile-kubeconfig.sh` so Kind, Lima, and Slicer keep their runtime-specific kubeconfig production local while sharing split-kubeconfig reconciliation through one executable adapter.
- Red test: `bats tests/kubernetes-kubeconfig-reconcile-adapter.bats` failed before implementation because the shared reconciler did not exist and the runtime scripts did not delegate to it.
- Verification: `bats tests/kubernetes-kubeconfig-reconcile-adapter.bats`; `bats kubernetes/kind/tests/ensure-kind-kubeconfig.bats`; `bats kubernetes/lima/tests/ssh-agent.bats`; `bats kubernetes/slicer/tests/bootstrap-k3s-slicer.bats`; `bats kubernetes/lima/tests/manage-kubeconfig.bats`; `bash -n kubernetes/scripts/reconcile-kubeconfig.sh kubernetes/kind/scripts/ensure-kind-kubeconfig.sh kubernetes/lima/scripts/bootstrap-k3s-lima.sh kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/reconcile-kubeconfig.sh --path kubernetes/kind/scripts/ensure-kind-kubeconfig.sh --path kubernetes/lima/scripts/bootstrap-k3s-lima.sh --path kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`; `shellcheck kubernetes/scripts/reconcile-kubeconfig.sh kubernetes/kind/scripts/ensure-kind-kubeconfig.sh kubernetes/lima/scripts/bootstrap-k3s-lima.sh kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`; `scripts/audit-shell-scripts.sh --execute --path kubernetes/scripts/reconcile-kubeconfig.sh --path kubernetes/kind/scripts/ensure-kind-kubeconfig.sh --path kubernetes/lima/scripts/bootstrap-k3s-lima.sh --path kubernetes/slicer/scripts/bootstrap-k3s-slicer.sh`; `bats kubernetes/kind/tests/makefile.bats`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`; stale policy scan leaves merge/delete actions only in the shared reconciler and Kind's split-kubeconfig validation local.
- Verdict: PRODUCTIVE

### Pass 46 - Shared helper-mode threshold policy - 2026-06-11T19:09:29Z

- Files changed: kubernetes/scripts/helper-mode-lib.sh, tests/kubernetes-helper-mode-lib.bats, kubernetes/kind/Makefile, kubernetes/lima/Makefile, kubernetes/slicer/Makefile, sites/docs/content/reference/shell-scripts.mdx
- Changes: Extracted the duplicated local-runtime helper-mode `auto`/`on`/`off` threshold policy from Kind, Lima, and Slicer apply recipes into `kubernetes/scripts/helper-mode-lib.sh`, leaving each Makefile to own only its runtime-specific helper actions and thresholds.
- Red test: `bats tests/kubernetes-helper-mode-lib.bats` failed before implementation because the shared helper-mode library did not exist.
- Verification: `bats tests/kubernetes-helper-mode-lib.bats`; `bats kubernetes/kind/tests/makefile.bats`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `bash -n kubernetes/scripts/helper-mode-lib.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/helper-mode-lib.sh`; `shellcheck kubernetes/scripts/helper-mode-lib.sh`; `rg -n "mode_enabled\\(\\)|helper_mode_enabled|HELPER_MODE_LIB" kubernetes/kind/Makefile kubernetes/lima/Makefile kubernetes/slicer/Makefile kubernetes/scripts/helper-mode-lib.sh`. The scout-suggested `make -n ... apply` probes were attempted but rejected as invalid verification because recursive Make/Terragrunt paths still reached real apply approval prompts; their recipe expansion did show all three runtimes sourcing `helper-mode-lib.sh` and calling `helper_mode_enabled`.
- Verdict: PRODUCTIVE

### Pass 47 - Shared local image helper planner - 2026-06-11T19:19:19Z

- Files changed: kubernetes/scripts/plan-local-image-helpers.sh, tests/kubernetes-local-image-helper-plan.bats, kubernetes/lima/Makefile, kubernetes/slicer/Makefile, kubernetes/lima/tests/makefile.bats, kubernetes/slicer/tests/makefile.bats, sites/docs/content/reference/shell-scripts.mdx
- Changes: Extracted Lima/Slicer stage-gated local image cache, platform image build, and workload image build planning into `kubernetes/scripts/plan-local-image-helpers.sh`. Lima and Slicer now consume a shared action plan while keeping runtime-specific Make targets local, and Lima gained the missing `ensure-image-cache` adapter so the shared plan normalizes the previous drift.
- Red test: `bats tests/kubernetes-local-image-helper-plan.bats` failed before implementation because the shared planner did not exist.
- Verification: `bats tests/kubernetes-local-image-helper-plan.bats`; `bats tests/kubernetes-helper-mode-lib.bats tests/kubernetes-local-image-helper-plan.bats tests/kubernetes-sync-image-cache-adapter.bats tests/kubernetes-workload-image-builder-adapter.bats tests/kubernetes-platform-image-builder-adapter.bats`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `bash -n kubernetes/scripts/plan-local-image-helpers.sh kubernetes/scripts/helper-mode-lib.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/plan-local-image-helpers.sh --path kubernetes/scripts/helper-mode-lib.sh`; `shellcheck kubernetes/scripts/plan-local-image-helpers.sh kubernetes/scripts/helper-mode-lib.sh`; `make -C kubernetes/lima help`; `make -C kubernetes/slicer help`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`; stale policy scan leaves the cache-off error and image-build skip text only in the shared planner, with Lima/Slicer Makefiles calling `PLAN_LOCAL_IMAGE_HELPERS`.
- Verification caveat: `scripts/audit-shell-scripts.sh --execute --path kubernetes/scripts/plan-local-image-helpers.sh --path kubernetes/scripts/helper-mode-lib.sh` reported zero scanned scripts because these new files are still untracked in the current dirty worktree; direct syntax, Bash 3.2, and shellcheck checks covered the new script.
- Verdict: PRODUCTIVE

### Pass 48 - Shared Slicer host access gate for read-only checks - 2026-06-11T19:29:06Z

- Files changed: kubernetes/slicer/Makefile, kubernetes/slicer/tests/makefile.bats
- Changes: Added `prepare-host-access` as the read-only host-forward gate for Slicer checks. It reuses `helper-mode-lib.sh` so `SLICER_HOST_FORWARDS_MODE=off` skips host-forward setup, while `auto` and `on` still delegate to `ensure-host-forwards` at the existing stage threshold. Browser-facing and readiness checks now call the gate instead of bypassing the shared helper-mode policy.
- Red test: `bats kubernetes/slicer/tests/makefile.bats --filter 'host forwards off mode'` failed before implementation because the `check-gateway-urls` preview still printed `ensure-host-forwards` with `SLICER_HOST_FORWARDS_MODE=off`.
- Verification: `bats kubernetes/slicer/tests/makefile.bats --filter 'host forwards off mode'`; `bats kubernetes/slicer/tests/makefile.bats --filter 'host forwards off mode|host access mode gating'`; `bats kubernetes/slicer/tests/makefile.bats`; `bats kubernetes/lima/tests/makefile.bats`; `make -n -C kubernetes/slicer check-gateway-urls STAGE=900 SLICER_HOST_FORWARDS_MODE=off APP=grafana | rg -n "prepare-host-access|ensure-host-forwards|check-gateway-urls"`; `make -n -C kubernetes/lima check-gateway-urls STAGE=900 LIMA_HOST_GATEWAY_PROXY_MODE=off | rg -n "ensure-host-gateway-proxy|check-gateway-urls"`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`; source scan confirmed read-only Slicer checks call `prepare-host-access` while apply-time direct host-forward setup remains local.
- Verdict: PRODUCTIVE

### Pass 49 - Shared stage-first read-only helper surface - 2026-06-11T19:36:41Z

- Files changed: kubernetes/stage-ladder.mk, kubernetes/kind/Makefile, kubernetes/lima/Makefile, kubernetes/slicer/Makefile, tests/kubernetes-stage-helper-surface.bats
- Changes: Added `KUBERNETES_STAGE_READONLY_HELPERS` to the shared stage ladder and pointed Kind, Lima, and Slicer `VALID_STAGE_HELPERS` at it. The stage-first read-only helper interface is now defined once, so `make 900 show-urls` and related helper dispatch policy no longer drifts across local Kubernetes variants.
- Red test: `bats tests/kubernetes-stage-helper-surface.bats` failed before implementation because stage-first `show-urls` was not recognized as a stage helper and the dry-run recipe expansion still showed the empty-helper `Stage 900 requires an action` guard.
- Verification: `bats tests/kubernetes-stage-helper-surface.bats`; `bats tests/kubernetes-stage-monotonicity-adapter.bats`; `bats kubernetes/kind/tests/makefile.bats`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `for variant in kind lima slicer; do make -n -C "kubernetes/$${variant}" 900 show-urls DRY_RUN=1 | rg -q 'if \[ -z "" \] && \[ -z "show-urls" \]; then' && make -n -C "kubernetes/$${variant}" 900 show-urls DRY_RUN=1 | rg -q -- '--show-urls' && echo "ok $${variant}"; done`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`.
- Verification caveat: Real `make -C kubernetes/slicer 900 show-urls DRY_RUN=1` still reaches existing live Slicer kubeconfig/reachability prechecks before the dry-run health script, so this pass verifies the shared stage-first dispatch interface with `make -n` for Slicer and does not change Slicer's precheck policy.
- Verdict: PRODUCTIVE

### Pass 50 - Shared k3s OIDC recovery drill interface - 2026-06-11T19:44:56Z

- Files changed: kubernetes/scripts/exercise-k3s-oidc-recovery.sh, kubernetes/lima/scripts/exercise-k3s-oidc-recovery.sh, kubernetes/slicer/scripts/exercise-k3s-oidc-recovery.sh, kubernetes/slicer/Makefile, kubernetes/slicer/tests/makefile.bats, kubernetes/slicer/tests/exercise-k3s-oidc-recovery.bats
- Changes: Extracted Lima's k3s OIDC recovery drill into a shared runtime-aware harness with `K3S_OIDC_RUNTIME=lima|slicer`, left Lima as a compatibility adapter, and added the missing Slicer public `exercise-k3s-oidc-recovery` target and wrapper. Slicer now has the same recovery drill interface, JSON/text preview contract, and force-mode knobs as Lima while keeping runtime transport local.
- Red test: `bats kubernetes/slicer/tests/makefile.bats --filter 'slicer exercise-k3s-oidc-recovery runs the shared k3s recovery harness'` failed before implementation because Slicer did not expose the `exercise-k3s-oidc-recovery` Make goal.
- Verification: `bats kubernetes/slicer/tests/makefile.bats --filter 'slicer exercise-k3s-oidc-recovery runs the shared k3s recovery harness'`; `bats kubernetes/lima/tests/makefile.bats --filter 'lima exercise-k3s-oidc-recovery runs the dedicated harness'`; `bats kubernetes/lima/tests/exercise-k3s-oidc-recovery.bats`; `bats kubernetes/slicer/tests/configure-k3s-apiserver-oidc.bats`; `bats kubernetes/slicer/tests/exercise-k3s-oidc-recovery.bats`; `bats kubernetes/lima/tests/exercise-k3s-oidc-recovery.bats kubernetes/slicer/tests/exercise-k3s-oidc-recovery.bats`; `bats kubernetes/slicer/tests/makefile.bats kubernetes/lima/tests/makefile.bats`; `bash -n kubernetes/scripts/exercise-k3s-oidc-recovery.sh kubernetes/lima/scripts/exercise-k3s-oidc-recovery.sh kubernetes/slicer/scripts/exercise-k3s-oidc-recovery.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/exercise-k3s-oidc-recovery.sh --path kubernetes/lima/scripts/exercise-k3s-oidc-recovery.sh --path kubernetes/slicer/scripts/exercise-k3s-oidc-recovery.sh`; `shellcheck kubernetes/scripts/exercise-k3s-oidc-recovery.sh kubernetes/lima/scripts/exercise-k3s-oidc-recovery.sh kubernetes/slicer/scripts/exercise-k3s-oidc-recovery.sh`; `scripts/audit-shell-scripts.sh --execute --path kubernetes/scripts/exercise-k3s-oidc-recovery.sh --path kubernetes/lima/scripts/exercise-k3s-oidc-recovery.sh --path kubernetes/slicer/scripts/exercise-k3s-oidc-recovery.sh`; `bats tests/docs-content-current.bats --filter 'shell|script|current|docs site'`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`.
- Verification note: The shell audit validated the tracked shared executable entrypoint; direct `bash -n`, Bash 3.2, shellcheck, and Bats covered the new untracked Slicer wrapper in this dirty worktree.
- Verdict: PRODUCTIVE

### Pass 51 - Variant-contract diagnostic dispatch for check-health - 2026-06-11T19:53:56Z

- Files changed: kubernetes/scripts/run-diagnostic-check.sh, kubernetes/kind/Makefile, kubernetes/lima/Makefile, kubernetes/slicer/Makefile, kubernetes/kind/tests/makefile.bats, kubernetes/lima/tests/makefile.bats, kubernetes/slicer/tests/makefile.bats, tests/kubernetes-diagnostic-dispatch.bats
- Changes: Added `run-diagnostic-check.sh` as a shared variant-contract-backed dispatcher for the first diagnostic action, `check-health`. Kind, Lima, and Slicer now keep their runtime-specific readiness gates local but delegate the final health diagnostic through one module that reads `cluster_access` and `host_access_path` from `variant.json`, expands kubeconfig paths, preserves ordered `--var-file` arguments, and dispatches to `check-cluster-health.sh`.
- Red test: `bats tests/kubernetes-diagnostic-dispatch.bats` failed before implementation because `check-health` still called the health script directly instead of delegating through `kubernetes/scripts/run-diagnostic-check.sh`.
- Verification: `bats tests/kubernetes-diagnostic-dispatch.bats`; `bats tests/variant-contracts.bats`; `bats kubernetes/kind/tests/makefile.bats --filter 'check-health'`; `bats kubernetes/lima/tests/makefile.bats --filter 'check-health'`; `bats kubernetes/slicer/tests/makefile.bats --filter 'check-health'`; `bats kubernetes/kind/tests/makefile.bats`; `bats kubernetes/lima/tests/makefile.bats`; `bats kubernetes/slicer/tests/makefile.bats`; `bash -n kubernetes/scripts/run-diagnostic-check.sh`; `shellcheck kubernetes/scripts/run-diagnostic-check.sh`; `scripts/check-bash32-compat.sh --execute --path kubernetes/scripts/run-diagnostic-check.sh`; `kubernetes/scripts/run-diagnostic-check.sh --help`; `kubernetes/scripts/run-diagnostic-check.sh --dry-run --variant-json kubernetes/variants/kind/variant.json --action check-health --stage 900`; `scripts/check-make-target-surfaces.sh --execute`; `git diff --check`.
- Verification note: `scripts/audit-shell-scripts.sh --execute --path kubernetes/scripts/run-diagnostic-check.sh` reported zero scanned scripts because the new dispatcher is untracked in the current dirty worktree; direct no-op interface, syntax, Bash 3.2, shellcheck, and Bats tests covered it.
- Verdict: PRODUCTIVE

---

## Prior Completed Loop

## Skill: extreme-software-optimization

## Target: /Users/nickromney/Developer/personal/platform

## Total Passes: 10

## Started: 2026-06-05T22:36:56Z

## Status: COMPLETE - 10 of 10 passes finished

## Stop Conditions

- Stop after two consecutive ZERO-CHANGE passes with named profiled targets and measured evidence.
- Stop on thrashing if an optimization makes the same output or timing surface unstable.
- Stop at pass 10 if convergence has not happened earlier.
- Only implement a lever with an opportunity score >= 2.0 and a golden-output proof.

## Missions

1. Workflow preview subprocess hotspots: Re-baseline the preset-heavy `scripts/platform-workflow.sh preview` path and optimize the top remaining measured subprocess hotspot.
2. Workflow options rendering: Profile option-contract rendering and remove repeated reads or `jq` fanout only if it is still material.
3. Image catalog projections: Profile image catalog ref/build projections used by local image workflows and optimize repeated catalog parsing if measured.
4. Platform status and inventory: Profile `platform-status`, read-model, and inventory composition for redundant probes or JSON fanout.
5. Version audit path: Profile component/provider/image version audit commands for repeated catalog or lockfile work.
6. Shell helper hot loops: Profile shared shell helper paths used by many tests and scripts for avoidable subprocesses.
7. Go app test/build path: Profile representative Go app build/test commands and optimize only behavior-preserving hot spots.
8. Python contract tests: Profile `tests/app_contracts.py`-backed suites for repeated filesystem scans or parsing.
9. Make target surfaces: Profile root and subtree Make discovery/help paths for repeated shell work.
10. Optimization convergence: Re-run the hottest remaining benchmarks and stop if no score >= 2.0 lever remains.

## Completed Passes

### Pass 1 - Workflow preview subprocess hotspots - 2026-06-05T22:41:33Z

- Files changed: scripts/platform-workflow.sh, tests/platform-workflow.bats, tests/artifacts/perf/20260605T223656Z-workflow-preview-pass1/*
- Changes: Profiled the preset-heavy workflow preview path, memoized pure variant registry host lookups for one invocation, and added a jq-wrapper test that keeps runtime/push host contract lookups to one each.
- Commit: 6613d37
- Baseline/Profile: `hyperfine --warmup 3 --runs 10` mean 196.3ms sigma 20.4ms; jq wrapper profile showed 36 jq invocations and 3 runtime-host contract lookups.
- Result: post-change mean 176.3ms sigma 11.7ms; jq invocations dropped to 34 and runtime-host contract lookups dropped to 1.
- Isomorphism proof: ordering preserved, tie-breaking unchanged, floating-point/RNG N/A, and `shasum -a 256 -c tests/artifacts/perf/20260605T223656Z-workflow-preview-pass1/golden_checksums.txt` passed.
- Verification: failing `bats --filter "platform workflow memoizes variant registry contract lookups" tests/platform-workflow.bats` before implementation; then focused memoization test; `bash -n scripts/platform-workflow.sh`; `shellcheck scripts/platform-workflow.sh`; `bats tests/platform-workflow.bats`; `bats tests/platform-workflow-matrix.bats`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 2 - Workflow options rendering - 2026-06-05T22:44:06Z

- Files changed: scripts/platform-workflow.sh, tests/platform-workflow.bats, tests/artifacts/perf/20260605T224133Z-workflow-options-pass2/*
- Changes: Profiled direct option rendering versus the public `platform-workflow options` command, then added an early `options` fast path after flag parsing so static option output skips preview validation, registry cache priming, and Make command construction.
- Commit: f02806b
- Baseline/Profile: direct renderer mean 17.9ms sigma 2.7ms; public options mean 65.1ms sigma 8.8ms; jq wrapper profile showed 10 jq invocations including registry and action-metadata lookups.
- Result: public options mean 35.3ms sigma 4.4ms; jq invocations dropped from 10 to 2 and registry lookups dropped to 0.
- Isomorphism proof: output order preserved by `print_options`, source options JSON unchanged, floating-point/RNG N/A, and `shasum -a 256 -c tests/artifacts/perf/20260605T224133Z-workflow-options-pass2/golden_checksums.txt` passed.
- Verification: failing `bats --filter "platform workflow options avoids preview command setup" tests/platform-workflow.bats` before implementation; then focused options fast-path test; `bash -n scripts/platform-workflow.sh`; `shellcheck scripts/platform-workflow.sh`; `bats tests/platform-workflow.bats`; `bats tests/platform-workflow-matrix.bats`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 3 - Image catalog projections - 2026-06-05T22:49:38Z

- Files changed: kubernetes/workflow/image-catalog-lib.sh, tests/image-catalog-lib.bats, tests/artifacts/perf/20260605T224406Z-image-catalog-pass3/*
- Changes: Profiled registry-mode kind operator override rendering, then cached source fingerprint tags by exact ordered source list in a temp file shared across command substitutions.
- Commit: 7656e8c
- Baseline/Profile: `render-operator-overrides.sh` registry mode mean 4.186s sigma 0.218s; subprocess profile showed 10 `find` scans and 409 `shasum` calls, with duplicate source lists for workload image pairs.
- Result: mean 3.327s sigma 0.106s; `find` scans dropped to 8 and `shasum` calls to 330.
- Isomorphism proof: image ref order preserved, cache key uses exact ordered source list, floating-point/RNG N/A, and `shasum -a 256 -c tests/artifacts/perf/20260605T224406Z-image-catalog-pass3/golden_checksums.txt` passed.
- Verification: failing `bats --filter "source tags cache repeated equivalent fingerprint sources" tests/image-catalog-lib.bats` before implementation; then focused cache test; `bash -n kubernetes/workflow/image-catalog-lib.sh`; `shellcheck kubernetes/workflow/image-catalog-lib.sh`; `bats tests/image-catalog-lib.bats`; `bats kubernetes/kind/tests/render-operator-overrides.bats`; `bats tests/validate-docker-optimization-contracts.bats`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 4 - Platform status and inventory - 2026-06-05T22:57:50Z

- Files changed: scripts/platform-inventory.sh, scripts/platform-status-read-model.sh, tests/platform-inventory.bats, tests/artifacts/perf/20260605T224938Z-status-inventory-pass4/*
- Changes: Profiled status/read-model/inventory composition, then let inventory pass its already-collected status JSON into the read-model projection instead of probing live status twice.
- Commit: fad1b6d
- Baseline/Profile: status mean 3.336s, read-model mean 3.306s, inventory mean 6.740s; inventory cost was nearly two status probes.
- Result: inventory mean 3.427s; status and standalone read-model remained effectively flat.
- Isomorphism proof: inventory ordering preserved, read-model input equals inventory `raw_status`, floating-point/RNG N/A, and recursively normalized `generated_at` golden checksums passed.
- Verification: failing `bats --filter "platform inventory reuses one status probe" tests/platform-inventory.bats` before implementation; then focused inventory test; `bash -n scripts/platform-inventory.sh scripts/platform-status-read-model.sh`; `shellcheck scripts/platform-inventory.sh scripts/platform-status-read-model.sh`; `bats tests/platform-inventory.bats`; `bats tests/platform-status-read-model.bats`; `bats tests/platform-status.bats`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 5 - Version audit path - 2026-06-05T23:06:15Z

- Files changed: scripts/check-repo-version.sh, tests/check-version.bats, tests/artifacts/perf/20260605T225750Z-version-audit-pass5/*
- Changes: Profiled the skipped-upstream version audit, then batched workflow action-reference parsing into one Python startup while keeping selector resolution and reporting in the shell loop.
- Commit: a786847
- Baseline/Profile: `CHECK_VERSION_SKIP_UPSTREAM=1 scripts/check-repo-version.sh --execute` mean 1.523s sigma 0.024s; subprocess profile showed 9 `uv run --isolated python - ...` calls, including 4 separate workflow parser startups.
- Result: mean 1.423s sigma 0.027s; `uv` calls dropped to 6 and workflow parser startups dropped to 1.
- Isomorphism proof: skipped-upstream output SHA-256 stayed `4a5012aa3535f60431c4721c168c04be44c98c36efb8d76af4175158f2510e59`; workflow ordering remains sourced from `workflow_files`; duplicate suppression remains per workflow file.
- Verification: failing `bats --filter "check-version batches workflow action parsing" tests/check-version.bats` before implementation; then focused batching test; `bash -n scripts/check-repo-version.sh`; `shellcheck scripts/check-repo-version.sh`; `bats tests/check-version.bats`; `bats tests/version-audit-workflow.bats`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 6 - Shell helper hot loops - 2026-06-05T23:16:54Z

- Files changed: scripts/audit-shell-scripts.sh, tests/audit-shell-scripts.bats, tests/artifacts/perf/20260605T230615Z-shell-audit-pass6/*
- Changes: Profiled the scoped shell audit used by stack `test-shell`, then replaced small in-memory interface-output `grep`/`awk` checks with Bash string and line parsing.
- Commit: 43e3e20
- Baseline/Profile: scoped shell audit mean 15.581s sigma 0.228s; helper profile showed `grep` 1804, `awk` 416, `mktemp` 710, `cat` 710, `rm` 355.
- Result: mean 13.273s sigma 0.570s; `grep` dropped to 924 and `awk` dropped to 96 while probe and file-scan counts stayed intentionally unchanged.
- Isomorphism proof: scoped audit output SHA-256 stayed `1bdce3140a7b0389ec10a3afef4d18f1b10d5e1bb3cb07e7d2fa8f198d2142d1`; same-line and next-line Usage parsing are preserved.
- Verification: failing `bats --filter "validates interface output without grep" tests/audit-shell-scripts.bats` before implementation; then focused no-grep/awk test; `bash -n scripts/audit-shell-scripts.sh`; `shellcheck scripts/audit-shell-scripts.sh`; `scripts/check-bash32-compat.sh --execute --path scripts/audit-shell-scripts.sh`; `bats tests/audit-shell-scripts.bats`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 7 - Go app test/build path - 2026-06-05T23:25:01Z

- Files changed: kubernetes/workflow/image-build-lib.sh, tests/validate-docker-optimization-contracts.bats, tests/artifacts/perf/20260605T231654Z-go-apps-pass7/*
- Changes: Profiled Go unit-test and image prebuild surfaces, then cached exact image prebuild command strings for one image-build process so repeated Go prebuild commands run once after a successful first run.
- Commit: 85ad112
- Baseline/Profile: explicit Go unit aggregate passed; duplicate Go prebuild fixture with cache disabled mean 283.2ms sigma 10.5ms; real `build-linux` prebuild path is currently blocked by local Go 1.26.4 cross-compilation errors for new runtime internals.
- Result: duplicate Go prebuild fixture mean 173.0ms sigma 9.4ms; fixture output SHA-256 stayed `1c1fead8dfc612640aaf46edc72fbb77a897a191a2bf2ef576110449f9c116d4`; identical prebuild command executions dropped from two to one while both image IDs still built.
- Isomorphism proof: cache key is exact command text, failed prebuild commands are not cached, and the build loop still visits both catalog entries.
- Verification: failing `bats --filter "prebuild hooks run once" tests/validate-docker-optimization-contracts.bats` before implementation; then focused prebuild test; `bash -n kubernetes/workflow/image-build-lib.sh`; `shellcheck kubernetes/workflow/image-build-lib.sh`; Go unit aggregate via `make -C apps ...`; `bats tests/validate-docker-optimization-contracts.bats`; `git diff --check`.
- Verdict: PRODUCTIVE with build-linux verification caveat from the local Go toolchain

### Pass 8 - Python contract tests - 2026-06-05T23:29:57Z

- Files changed: tests/validate-docker-optimization-contracts.bats, tests/artifacts/perf/20260605T232501Z-python-contracts-pass8/*
- Changes: Profiled the Python-backed Docker optimization contract suite, then replaced per-assertion `uv run --isolated` launches with direct `python3` launches using the already-available repository Python environment.
- Commit: ba57da3
- Baseline/Profile: full contract suite mean 4.226s sigma 0.058s; direct `uv` startup also required escalation in this sandbox because it attempted to initialize `/Users/nickromney/.cache/uv`.
- Result: mean 3.938s sigma 0.055s; runtime dropped by 0.288s (-6.8%); `uv run --isolated` call sites in `tests/validate-docker-optimization-contracts.bats` dropped to 0.
- Isomorphism proof: Bats TAP output SHA-256 stayed `1c54cecead4083347612c9df0cb202bfedb98bf45dbfb6819fcf6e35c880ea3a`; PyYAML 6.0.3 is available through `python3`, preserving the compose contract parser dependency.
- Verification: `python3 -B -m py_compile tests/app_contracts.py`; `bats tests/validate-docker-optimization-contracts.bats`; `shasum -a 256 -c tests/artifacts/perf/20260605T232501Z-python-contracts-pass8/golden_checksums.txt`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 9 - Make target surfaces - 2026-06-05T23:42:18Z

- Files changed: apps/Makefile, apps/sentiment/Makefile, docker/compose/Makefile, tests/app_contracts.py, tests/apps-makefile.bats, tests/makefile.bats, tests/sentiment-makefile.bats, tests/artifacts/perf/20260605T233115Z-make-surfaces-pass9/*
- Changes: Profiled root/subtree Make help surfaces, then made app wrapper discovery and compose backend command wrappers lazy so static help no longer performs target scans or backend detection.
- Commit: 3499629
- Baseline/Profile: apps help mean 129.1ms sigma 24.2ms; docker/compose help mean 319.2ms sigma 23.7ms; root and Kubernetes help surfaces had no targeted opportunity in this pass.
- Result: apps help mean 9.0ms sigma 0.3ms (-93.0%); docker/compose help mean 12.7ms sigma 6.7ms (-96.0%); root help stayed flat.
- Isomorphism proof: measured help output checksums stayed identical before/after; execute-mode compose resolution still logs the backend twice in the existing stubbed test path; app delegation targets still scan wrappers when invoked.
- Verification: failing focused help laziness tests before implementation; then `bats tests/makefile.bats tests/apps-makefile.bats tests/sentiment-makefile.bats tests/make-target-surfaces.bats`; `python3 -B -m py_compile tests/app_contracts.py`; `shasum -a 256 -c tests/artifacts/perf/20260605T233115Z-make-surfaces-pass9/golden_checksums.txt`; `git diff --check`.
- Verdict: PRODUCTIVE

### Pass 10 - Optimization convergence - 2026-06-05T23:50:15Z

- Files changed: scripts/audit-shell-scripts.sh, tests/audit-shell-scripts.bats, tests/artifacts/perf/20260605T233559Z-convergence-pass10/*
- Changes: Re-ran the hottest remaining surfaces, found one final shell-audit capture hotspot, then reused one stdout/stderr capture pair per audit process and read probe output with Bash builtins.
- Commit: 97c683e
- Baseline/Profile: convergence sweep showed scoped shell audit at 13.898s sigma 0.437s, image catalog at 3.268s, platform inventory at 3.441s, Docker optimization contracts at 3.981s, and kind help at 135.7ms.
- Result: scoped shell audit mean 10.738s sigma 0.544s; runtime dropped by 3.159s (-22.7%) from the pass-10 convergence baseline.
- Isomorphism proof: scoped audit output SHA-256 stayed `1bdce3140a7b0389ec10a3afef4d18f1b10d5e1bb3cb07e7d2fa8f198d2142d1`; stderr-first failure extraction is preserved by separate capture files.
- Verification: focused failing capture-reuse test before implementation; then `shellcheck scripts/audit-shell-scripts.sh`; `scripts/check-bash32-compat.sh --execute --path scripts/audit-shell-scripts.sh`; `bats tests/audit-shell-scripts.bats`; `shasum -a 256 -c tests/artifacts/perf/20260605T233559Z-convergence-pass10/golden_checksums.txt`; `git diff --check`.
- Verdict: PRODUCTIVE and final pass complete
