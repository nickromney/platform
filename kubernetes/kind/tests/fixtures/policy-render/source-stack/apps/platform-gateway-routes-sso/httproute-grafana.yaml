apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
spec:
  hostnames:
    - grafana.admin.127.0.0.1.sslip.io
  rules:
    - backendRefs:
        - name: oauth2-proxy-grafana
          namespace: sso
