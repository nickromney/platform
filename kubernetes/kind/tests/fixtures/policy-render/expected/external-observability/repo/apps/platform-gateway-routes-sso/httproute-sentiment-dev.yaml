apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sentiment-dev
spec:
  hostnames:
    - sentiment.dev.obs.example.test
  rules:
    - backendRefs:
        - name: oauth2-proxy-sentiment-dev
          namespace: sso
