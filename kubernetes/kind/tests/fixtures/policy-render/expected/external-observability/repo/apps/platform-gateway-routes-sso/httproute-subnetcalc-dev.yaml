apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: subnetcalc-dev
spec:
  hostnames:
    - subnetcalc.dev.obs.example.test
  rules:
    - backendRefs:
        - name: oauth2-proxy-subnetcalc-dev
          namespace: sso
