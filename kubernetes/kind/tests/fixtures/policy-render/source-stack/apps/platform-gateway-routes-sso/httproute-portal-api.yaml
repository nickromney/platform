apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: portal-api
spec:
  hostnames:
    - portal-api.127.0.0.1.sslip.io
  rules:
    - backendRefs:
        - name: oauth2-proxy-idp-core
          namespace: sso
