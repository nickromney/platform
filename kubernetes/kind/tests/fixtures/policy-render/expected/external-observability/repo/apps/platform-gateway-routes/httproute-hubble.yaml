apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hubble
spec:
  hostnames:
    - hubble.admin.obs.example.test
  rules:
    - backendRefs:
        - name: oauth2-proxy-hubble
          namespace: sso
