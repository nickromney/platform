{
  "name": "platform-e2e-sso",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright smoke tests for platform SSO-protected endpoints (OIDC provider + oauth2-proxy + Headlamp OIDC).",
  "packageManager": "bun@1.3.10",
  "license": "SEE LICENSE IN ../../../LICENSE.md",
  "scripts": {
    "test": "playwright test --project=smoke",
    "test:working": "playwright test --project=working",
    "test:headed": "playwright test --headed --project=smoke",
    "test:working:headed": "playwright test --headed --project=working",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "^1.57.0"
  }
}
