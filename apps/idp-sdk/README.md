# IDP SDK

Small typed client for the Portal API.

This package intentionally has no runtime or development dependencies. It wraps
browser-native `fetch` and keeps the API surface limited to catalog reads,
runtime reads, environment dry-runs, and deployment promotion dry-runs.
