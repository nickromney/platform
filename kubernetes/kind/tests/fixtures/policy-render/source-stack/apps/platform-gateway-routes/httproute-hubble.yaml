apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hubble
spec:
  hostnames:
    - hubble.admin.127.0.0.1.sslip.io
  rules:
    - backendRefs:
        - name: oauth2-proxy-hubble
          namespace: sso
