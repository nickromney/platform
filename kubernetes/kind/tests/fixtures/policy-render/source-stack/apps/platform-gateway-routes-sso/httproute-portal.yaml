apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: portal
spec:
  hostnames:
    - portal.127.0.0.1.sslip.io
  rules:
    - backendRefs:
        - name: oauth2-proxy-backstage
          namespace: sso
