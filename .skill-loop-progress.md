# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: /Users/nickromney/Developer/personal/platform
# Total Passes: 10
# Started: 2026-06-05T22:36:56Z

## Status: IN PROGRESS - Pass 1 of 10

## Stop Conditions
- Stop after two consecutive ZERO-CHANGE passes with named profiled targets and measured evidence.
- Stop on thrashing if an optimization makes the same output or timing surface unstable.
- Stop at pass 10 if convergence has not happened earlier.
- Only implement a lever with an opportunity score >= 2.0 and a golden-output proof.

## Missions
1. Workflow preview subprocess hotspots: Re-baseline the preset-heavy `scripts/platform-workflow.sh preview` path and optimize the top remaining measured subprocess hotspot.
2. Workflow options rendering: Profile option-contract rendering and remove repeated reads or `jq` fanout only if it is still material.
3. Image catalog projections: Profile image catalog ref/build projections used by local image workflows and optimize repeated catalog parsing if measured.
4. Platform status and inventory: Profile `platform-status`, read-model, and inventory composition for redundant probes or JSON fanout.
5. Version audit path: Profile component/provider/image version audit commands for repeated catalog or lockfile work.
6. Shell helper hot loops: Profile shared shell helper paths used by many tests and scripts for avoidable subprocesses.
7. Go app test/build path: Profile representative Go app build/test commands and optimize only behavior-preserving hot spots.
8. Python contract tests: Profile `tests/app_contracts.py`-backed suites for repeated filesystem scans or parsing.
9. Make target surfaces: Profile root and subtree Make discovery/help paths for repeated shell work.
10. Optimization convergence: Re-run the hottest remaining benchmarks and stop if no score >= 2.0 lever remains.

## Completed Passes
(none yet)
