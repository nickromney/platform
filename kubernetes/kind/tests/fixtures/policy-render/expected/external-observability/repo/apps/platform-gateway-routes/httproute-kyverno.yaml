apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kyverno
spec:
  hostnames:
    - kyverno.admin.obs.example.test
  rules:
    - backendRefs:
        - name: policy-reporter
          namespace: policy-reporter
