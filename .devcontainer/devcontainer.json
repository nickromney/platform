{
  "name": "platform",
  "initializeCommand": "/bin/sh -lc 'mkdir -p \"$HOME/.kube\" \"$HOME/.config/platform-devcontainer/mkcert\"'",
  "workspaceMount": "source=${localWorkspaceFolder},target=${localWorkspaceFolder},type=bind,consistency=cached",
  "workspaceFolder": "${localWorkspaceFolder}",
  "build": {
    "context": ".",
    "dockerfile": "Dockerfile"
  },
  "features": {
    "ghcr.io/devcontainers/features/docker-outside-of-docker:1.9.1": {
      "version": "29.4.1",
      "mobyBuildxVersion": "0.33.0",
      "moby": true,
      "dockerDashComposeVersion": "v2"
    },
    "ghcr.io/devcontainers/features/node:1.7.1": {
      "version": "24.15.0",
      "pnpmVersion": "none",
      "nvmVersion": "0.40.4"
    }
  },
  "remoteUser": "vscode",
  "updateRemoteUserUID": true,
  "overrideCommand": true,
  "mounts": [
    "source=${localEnv:HOME}/.kube,target=${localEnv:HOME}/.kube,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.kube,target=/home/vscode/.kube,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.config/platform-devcontainer/mkcert,target=/home/vscode/.mkcert,type=bind,consistency=cached"
  ],
  "containerEnv": {
    "PLATFORM_DEVCONTAINER": "1",
    "PLATFORM_DEVCONTAINER_HOST_ALIAS": "host.docker.internal",
    "KIND_DEVCONTAINER_HOST_ALIAS": "host.docker.internal",
    "CAROOT": "/home/vscode/.mkcert",
    "DEFAULT_KUBECONFIG_PATH": "${localEnv:HOME}/.kube/config",
    "KIND_KUBECONFIG_PATH": "${localEnv:HOME}/.kube/kind-kind-local.yaml",
    "KUBECONFIG_PATH": "${localEnv:HOME}/.kube/kind-kind-local.yaml"
  },
  "postCreateCommand": "bash .devcontainer/post-create.sh",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "zsh"
      },
      "extensions": [
        "hashicorp.terraform",
        "ms-azuretools.vscode-containers",
        "ms-kubernetes-tools.vscode-kubernetes-tools",
        "redhat.vscode-yaml",
        "tamasfe.even-better-toml"
      ]
    }
  }
}
