# Known Subnetcalc API Conformance Divergences

There are no accepted divergences for the current process-based conformance
cases.

Rules for future entries:

- Assign a sequential ID such as `DISC-001`.
- Mark the status as `ACCEPTED`, `INVESTIGATING`, or `WILL-FIX`.
- List affected conformance case IDs.
- Include the reason, impact, and review date.
- Use expected-failure handling in the harness instead of silently skipping the
  case.
